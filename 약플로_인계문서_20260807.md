# 약플로(Yakflo) 인계문서 — 2026-08-07

> 약제과 약품관리 SaaS. 100~150병상 재활/요양병원, 약사 1~2인 대상. React+Vite / Supabase / Netlify.
> 배포: **yakflo.ehwaa.com** ← `origin/main` 자동배포. 저장소: ilovemeriel-netizen/yakflo-pharmacy.

---

## 0. 현재 상태 (2026-08-07)

| 항목 | 값 |
|---|---|
| main HEAD | **`0ebd98e`** (#250 사용 점검 화면 병합, 2026-08-27 17:39 KST) ~~`763fcc0`~~ ← 구 기재 |
| 운영 번들 | **`index-CJvlgDwX.js`** (2026-08-27 실측: 라이브=로컬 빌드 sha256 일치, 에셋 6종 전부 일치) ~~`index-BghB7LS1.js`~~ ← 구 기재 |
| 롤백 후보 | `5006af1` (직전 안정) |
| 원격 브랜치 | **main 단일** (stale 78개 삭제 완료) |
| 재고 변경 경로 | 4곳 전부 **조정거래 RPC 경유**로 전환 완료(PR #175) |
| 보고서 3경로 | 화면·인쇄·엑셀 **정본 소스 통일** 완료 |
| 단가 기준 | 전 경로 **`purchase_price`(구입단가/개당)** 통일 |
| ATC 화면 | 배치도(현재 배치·사용량순 제안)·슬롯 편집(교환·배정·제거)·**수기 조제 관리**(툴바·모달·지정/해제)·인쇄 2장(가나다)·전 표 StandardTable 표준화·양 뷰 검색(강조/흐리게·표 필터) (#190·#191) |

### 마이그레이션 적용 상태
| 마이그 | 내용 | 상태 |
|---|---|---|
| 0052 / 0054 | 마감월 가드(INSERT·UPDATE·DELETE 차단, plain date extract) | **적용** |
| 0055 | `drugs.current_qty` 직접 UPDATE 차단(거래 경유만) | **적용 완료(2026-08-23)** ~~미적용 ← 실사용 확인 후 apply~~ ← 구 기재(오류) |
| 0056 | `drugs.updated_at` + `drug_qty_audit` 감사 | **적용** |
| 0057 | `bulk_stock_adjust` RPC(서버 트랜잭션 재고조정) | **적용** |
| 0058 | junk 테이블 `"create_drug_lots.sql"` DROP | **적용**(PR #176) |
| 0059 / 0060 | `tenant_id` NOT NULL(4테이블 + `monthly_report_totals` 트리거) | **적용** |
| 0061 | 사용량 3컬럼(`prev_year_usage`·`recent_3m_usage`·`recent_1m_usage`) integer→numeric | **적용** |
| 0062 / 0063 | `drug_lots`→`drugs.expiry_date` 최단 유효기한 캐시 트리거 + UNIQUE(tenant_id,drug_code,lot_no) | **적용** |
| 0064 | ATC 카세트 컬럼(`atc_slot`·`fsp_slot`·`lasa_type`·`storage_light`) + 부분 UNIQUE 2종 | **적용** |
| 0065 | 상비 슬롯 초기 교환/`atc_pinned` 지정(※ 현재는 슬롯 고정 아님·사용량순 재산정 대상) | **적용** |
| 0066 | `swap_atc_slots` RPC(카세트 2칸 교환·원자적) | **적용** |
| 0067 | `fsp_note`(FSP 0.5T 3건 표기) | **적용** |
| 0068 | `assign_atc_slot`·`clear_atc_slot` RPC 초판(상비 차단) | **별도 apply 불요** — 0069가 두 함수를 `create or replace`로 대체(조치 불요, 미결 아님) ~~미적용(0069로 대체)~~ |
| **0069** (`0069_slot_rpc_pinned_movable.sql`) | **0068의 상비 차단 규칙 해제 — 상비 슬롯 이동·제거 허용(경고 후 진행). FSP 3건(FSP2·4·5, 0.5T)은 계속 차단.** `create or replace`로 0068 함수 대체 | **적용** |
| **0070** (`0070_fsp5_note_clear.sql`) | **FSP5(QROKEL125 큐로켈정12.5mg) `fsp_note`=NULL — FSP5는 0.5T 전용이 아니라 다빈도 배정 자리임이 확인됨. FSP2(LSX)·FSP4(SBCLP1)은 '0.5T' 유지. fsp_slot 3건·정본 무영향.** dryrun→승인→apply | **적용** |
| **0071** (`0071_atc_excluded.sql`) | **수기 조제(배정 제외) — `atc_excluded`(bool def false)·`atc_exclude_reason`(text) 컬럼 추가. true면 사용량순 배정에서 건너뜀(향정·마약과 동일 취급), 순위 산정엔 포함. 사유 4종(습기 취약·분할 불가·수기 조제·기타). 툴바 「수기 조제 관리」에서 지정/해제. 초기 적재 없음(사용자 지정). 정본 무영향 DDL.** dryrun→승인→apply(#191) | **적용** |

| **0078** (`0078_bulk_stock_adjust_round_amount.sql`) | `bulk_stock_adjust`의 `total_amount` 대입에 **`round()` 명시**(동작 동일·명시화 개선). RPC는 원래 실패 경로가 아니었음 | **적용**(2026-08-27) |
| **0079** (`0079_amount_columns_numeric.sql`) | **금액 3컬럼 bigint→numeric** — `transactions.total_amount`·`drugs.current_amount`·`inventory_stock.current_amount`. 수량 계열(numeric)과 타입 대칭·22P02 소멸·1원 편차 소멸 | **적용**(2026-08-27, #246 `de53ffc`) — 2026-08-27 실측 3컬럼 전부 numeric 확인 |
| **0080** (`0080_drug_idle_reviews.sql`) | **사용 점검 이력 테이블 신설** `drug_idle_reviews`(8컬럼·RLS 4정책·`trg_set_tenant_id`·인덱스 `(tenant_id,drug_code,reviewed_at desc)`). UNIQUE·CHECK·anon GRANT **의도적 미부여**(§5-17) | **적용**(2026-08-27, #249 `2fa94b2`) |

> ※ `atc_pinned` 의미 = **기계 잔류 보장(top88 강제 포함)**, 슬롯 고정 아님. 상비 3건은 사용량순 위치. **FSP 3칸은 0.5T 전용이 아님 — FSP2(LSX)·FSP4(SBCLP1)만 0.5T, FSP5(QROKEL125)는 다빈도 배정(0070)**. fsp_slot 배정(FSP2·4·5)은 서버 고정 유지. ~~유일 미적용 = 0055.~~ ← 구 기재(오류): 0055 적용 완료(2026-08-23). ~~0000~0076 전량 적용, 미적용 0건~~ ← **구 기재(2026-08-27 정정, 아래 참조)**.
>
> ### ★ 마이그레이션 적용 현황 정정 (2026-08-27 실측)
>
> **`0000~0080` 중 `0005`만 미적용. 그 외 전부 적용.** (`0003` 파일은 **존재하지 않음** — 해당 마이그는 0059+0060.) 0068은 0069가 `create or replace`로 대체하여 별도 apply 불요.
>
> - ⚠️ **0005 미적용 (2026-08-27 발견)** — `0005_profiles_admin_update.sql`가 만드는 **`profiles_update_admin` 정책이 라이브에 없다.**
>   - **현재 `profiles` 정책 3개**: `profiles_select_admin_all` · `profiles_select_own` · `profiles_update_own` (0005 적용 시 4개여야 함).
>   - **경위(추정)**: 서울 리전 이전 시 `0000_baseline`(구 DB 덤프)으로 재구축한 뒤 **0006부터 개별 apply**해, 0005만 재적용 대상에서 누락된 것으로 보임.
>   - **실무 영향**: **관리자가 다른 사용자의 profile을 수정할 수 없다**(본인 행만 가능). **사용자 1명 상태에서는 무해**하나, **member 온보딩 시 필요해진다.**
>   - ✅ **보안 영향 없음** — `0077` role 자가승격 가드는 **정상 존재**(실측 확인).
>   - → 조치는 **§2 [장기 · SaaS 확장] member role 격리 실증**에 **함께 처리 후보**로 등록. (지금 재적용하지 않음.)
>
> - ℹ️ **`supabase_migrations.schema_migrations`는 0행** — 이 프로젝트는 **SQL Editor / pg 스크립트 직접 apply** 방식이라 **DB에 적용 추적 이력이 남지 않는다.** 즉 **이 테이블은 적용 여부의 권위가 아니다.** 판정은 **객체 존재(테이블·함수·트리거·인덱스·정책·추가컬럼)로만** 가능하며, 객체를 만들지 않는 설정성 마이그(0007·0012·0013·0021·0024·0027·0036·0042·0058·0059·0061·0063·0070·0076·0079)는 **컬럼 타입·RLS 상태 등 별도 지문**으로 확인해야 한다.

---

## 1. 완료 이력 (인계 미완료 → 해소, 2026-08-07 실측)

| # | 항목 | 이전 | 현재 | 비고 |
|---|---|---|---|---|
| 1 | saas-migration push+DATA_API_KEY+재배포 | 미완료 | **완료** | 완전 병합·배포, env 존재 |
| 5 | 공유 레퍼런스 7테이블 RLS(인증 SELECT) | 미완료 | **완료** | RLS 활성 + SELECT 정책 |
| 10 | Drug360 저장 후 목록 갱신 버그 | 미완료 | **수정됨** | onSaved/onReload 콜백 |
| 11 | drug_master 매칭 커버리지 | 80.4% | **90.5%** (1,011/1,117) | 코드보유 기준 |
| 12 | 기준미설정 품목 | 70건 | **65건** | 사용·휴면 safety≤0 |
| 13 | 거래 soft-delete | 미완료 | **미도입 확정** | 전체삭제 버튼 제거로 원인 해소 |
| 9 | 도매사 매핑 | 진행중 | **현행 유지 확정** | 특정 품목만(32/1,117) |

### 그 외 최근 완료
- 보고서 건수=거래 행수 통일·인쇄본 A4 세로·명세 유효기한 열·엑셀 서식/음수괄호.
- 재고 직접쓰기 4경로(BulkUploadModal·drugBulk·DrugForm·DrugRegister) → `bulk_stock_adjust` RPC 전환(PR #175).
- 6월 monthly_snapshots per-item 흐름 **821행 복원**(엑셀결재본 CSV 825행 기준, 4코드 스냅샷 부재로 미복원).
- **ATC 인쇄 빈 페이지 해결(PR #239, 2026-08-24)** — 증상: 3용지·2장 백지. 원인: atc-page 고정 46행 분할 + `page-break-after:always`(min-height:100vh는 중간 백지 원인 아님 — 측정으로 확인). 해법: 고정 분할 폐기 → 단일 표 CSS 자동 흐름 + `thead{display:table-header-group}`로 헤더 반복 + `@media print`에서 min-height 해제(말미 잉여 페이지 예방). **slice 매직넘버 없이 해결 → 카세트 수·프린터 변경에 무관**.
- **약품 등록/수정 모달 결함 4건 해결(PR #243, 26d30f6, 2026-08-24)** — ① **API 조회 클릭 무응답**: `onClick={lookupApi}`로 PointerEvent가 `overrideName` 인자로 전달 → searchName이 이벤트 객체 → `nm.replace` TypeError(Enter는 무인자라 정상이었음). 수정 `onClick={()=>lookupApi()}`. ② **수정 모달 고위험 지정 불가**(UI 부재 + `ud` 미포함) → 체크박스(owner) + `ud.is_high_alert` 병합. ③ **고위험 초기값 미매핑**(저장은 되나 재진입 시 해제로 보여 타 필드만 수정해도 지정 풀림 — 조용한 데이터 손실) → 폼 초기화 `is_high_alert: !!dr.is_high_alert`. ④ **additive·compound_type이 수정 모달에 보이나 `ud`에 없어 저장 안 됨** → 병합 추가(additive는 빈값 병합, [[함정 #12]]). ※ 시드 2건(대한염화칼륨-40 `7PTSMCLDH`·대한염화나트륨-40주사액 `7NACLDH`) 배지 유지 확인 — 손실 없음.
- **GNB 즐겨찾기 기능(PR #245·#246·#247, 2026-08-24)** — GNB 하단 전용 행(`.cnc-fav`·`.no-print`·gnbH ResizeObserver 자동보정) + 지정 모달. 상한 **10개**·2행 줄바꿈(flexWrap)·순서 변경(**Pointer Events 드래그 + ↑↓**)·창 드래그(**훅A**·제목줄 한정). 저장 = `profiles.settings.favorites`(화면 id 배열·**스프레드 부분 갱신**으로 drugCols/changeCols 보존). 후보=ms children+단독(부모 그룹라벨 제외·alerts 이모지 제거). GnbNav·표 무수정.
- **DrugEditModal 경계 처리(PR #247)** — 인라인 드래그에 clamp 부재로 창이 화면 밖으로 이탈해 되돌릴 수 없던 문제 해소. **훅A와 동일한 EDGE 40px 계산식** 적용(box ref 측정→onMove 클램프). 인라인 유지(훅 교체 아님)·필드/저장 무변경([[함정 #13]]).
- **profiles.role 자가승격 차단(0077, PR #249, 2026-08-24 apply)** — 문제: `profiles_update_own`에 WITH CHECK 없음(`USING(auth.uid()=id)`만) → 일반 사용자가 자기 role을 admin으로 변경 가능(권한 상승). ※ **RLS WITH CHECK는 OLD 참조 불가**라 role만 차단하는 정책 작성이 구조적으로 불가능(`role='user'` 고정식 강제 시 admin의 settings self-update까지 막혀 즐겨찾기·컬럼 설정이 죽음) → 정책 수정이 아닌 **가드 트리거**로 해결. 조치: `trg_guard_profiles_role_direct`(BEFORE UPDATE, 0055 패턴)·함수 `guard_profiles_role_direct`, 차단 조건 **`NEW.role IS DISTINCT FROM OLD.role AND current_user='authenticated' AND NOT is_admin()` → 23514**, 통과 경로 **service_role(네이버 콜백·관리 스크립트)·postgres·is_admin()**. 검증: dryrun A~K + apply 후 재검증 9항목 전항 통과(settings self-update·drugCols/changeCols 무영향 실측·행수 1·정책 71·정본 885,285,628/106,365,758). ※ **`profiles_update_own` 정책 원본 그대로**(정책=「자기 행만」, 트리거=「단 role 제외」 분담). 롤백: `drop trigger trg_guard_profiles_role_direct on public.profiles;` + `drop function guard_profiles_role_direct();`(실행 시 취약점 재노출 — 실제 이상 확인 시에만). 배경: 네이버 콜백이 disable_signup 우회 계정 생성이 가능해 위험도 상향으로 즉시 apply([[네이버 계정 생성 제한]]).
- **drug_master 자동입력 결함 2건 해결(PR #240, 48b5787, 2026-08-26)** — **A)** 보험코드로 채운 값이 **약품명 Enter 시 소실** — 원인: `lookupApi` 진입부의 **조건 없는 8필드 리셋**([[함정 #14]]). **B)** 같은 보험코드 **재입력 시 재충전 불가** — 원인: `useEffect [insurance_code]` 의존성 값 불변으로 미재실행. **조치**: 리셋 제거(기존 값 보호) + **[마스터 조회] 버튼** 신설(재조회 명시 트리거·DrugEditModal/DrugRegister 양쪽) + **`product_code` 조회 키** 추가(보험코드→품목기준코드 1:1→약품명, 커버리지 68%→100%). 미변경: `lookupApi` API 호출·파싱 / `_mergeDrugMaster` 채움 정책(빈값만) / drug_master.`standard_code` 컬럼(미사용) / ud payload / 필드 순서·라벨 / 신규 hex 0. ※ **#243와 무관** — 최근 7일 drug_master 로직 변경 0건으로 확인됨(회귀 아닌 기존 결함).
- **`efficacy_class` 저장 불가 해소(0081 apply, PR #262 `993a9d0`, 2026-08-28)** — **증상**: 약품 등록 시 공공API가 「약효분류명」에 **「소화기계용약」 등을 채워 주는데**, 저장 후 재진입하면 **빈칸**. 오류도 뜨지 않음. **원인**: `drugs`에 **`efficacy_class` 컬럼이 없는데** 코드는 **10곳**(입력칸 3·저장 payload 3·API 매핑 2·폼 상태 2)이 **완비**돼 있었음 → PostgREST가 PGRST204를 반환하면 **0051 계열 폴백이 키를 제거하고 재시도해 「성공」으로 처리**([[함정 #22]]). 표 컬럼(`DRUG_COL_DEFS`)에도 없어 빈 값이 드러나지 않았음. ※ `efficacy_class`는 **마이그레이션 이력에 한 번도 없었다**(처음부터 부재) — `0000_baseline`의 `efficacy`(값 4/1,114건)와는 **별개 컬럼**. **조치**: `0081_drugs_efficacy_class.sql` — **컬럼 추가 1줄**(`text`·NULL 허용·DEFAULT/CHECK 없음). ★ **src 변경 0줄** — 컬럼만 생기면 즉시 동작. **NULL 허용 근거**: 코드가 `f.efficacy_class || null`로 **빈값을 NULL로 보내므로 NOT NULL이면 빈칸 저장이 23502로 실패**(그 외 3가지는 MIGRATION_LOG). dryrun A~I + ROLLBACK 전항 통과 후 승인 apply, **재검증 9항목 통과**(컬럼 text·nullable · **저장 3경로 폴백 없이 통과** · 값 저장·재조회 유지 후 **원복 확인** · 기존 NULL 1,114/1,114 · 행수 1,114 · RLS 4 · 컬럼 70→71·인덱스 4 · 정본 **885,285,628.424 / 106,365,758.469** · 검증행 잔류 0). **backfill 미실행** — `drug_master.drug_class`는 **숫자 코드**(112·113·214…)라 **명칭 필드인 `efficacy_class`와 층위가 다르고**(그대로 넣으면 화면에 `112`가 표시됨), 매칭 **910건 중 909건(99.9%)이 이미 ATC 분류를 보유**해 새로 생기는 정보가 없음. → NULL로 시작하고 **신규 등록·수정분부터 API가 채우게 둠**. 롤백: `alter table public.drugs drop column if exists efficacy_class;` ⚠ **값이 저장되기 시작한 뒤에는 그 값이 소실.**
- **영문 성분명 검색 불가 해소(PR #260 `104be88`, 2026-08-28)** — **증상**: 약품목록에서 「Vita」 검색 **0건**, 「비타민C」(한글)는 정상, 화면에는 성분(영문) 「Vitamin C」가 **정상 표시**(데이터·표시는 멀쩡). **원인**: `matchesDrugSearch`가 보는 4필드(`drug_name`·`drug_code`·`ingredient_kr`·`manufacturer`)에 **`ingredient_en` 누락**. ★ **버그라기보다 경로별 범위 차이** — **전역검색(`GLOBAL_SEARCH_FIELDS`)에는 처음부터 있었다**([[함정 #21]]). **조치**: 필드 **1개 추가**(실질 1줄 · `App.jsx` **diff 0건** — 검색 로직이 `src/lib/drugFilter.js`로 분리돼 있어 화면 코드 무수정). **검증**: 운영 DB 실측 전후 대조 — 「Vita」·「vita」·「vitamin c」 **0→1건**(고려은단비타민씨정), 「비타민c」 1→1·「가바로닌」 2→2·「대웅」 31→31 **무변동**. 기존 테스트 **14건 전부 통과**(vitest). ※ 「c」 1글자는 341→720건(기본 필터 「사용」 기준 151→353) — **최소 글자 수 제한 없음이 사용자 확정 사양**이라 정상. ※ 나머지 6필드는 **「첫 클릭 무반응」 계측 후 일괄**, `atc_code`·`atc_l1`·자유텍스트는 **제외 확정**.
- **금액 3컬럼 numeric 전환(0079 apply, PR #246 `de53ffc`, 2026-08-27)** — `transactions.total_amount`·`drugs.current_amount`·`inventory_stock.current_amount` **bigint→numeric**([[함정 #15]] 근본 해소). 효과: 소수 금액 삽입의 22P02 실패 소멸·프론트(half→+∞)/DB(half away) **1원 편차 소멸**·수량 계열(numeric)과 **타입 대칭**. **영향조사**: `current_amount` 기입 함수/트리거 **0개**(`drugs.current_amount` 합=0 레거시), `total_amount`는 `bulk_stock_adjust`만 참조, 뷰·인덱스·제약 0. ★ `monthly_snapshots` amount 5종은 **이미 numeric이라 범위 밖**. apply 소요 **77ms**. dryrun A~L + apply 후 재검증 11항목 통과(3컬럼 numeric·정본 **885,285,628.424 / 106,365,758.469** 무변동). ⚠ **롤백은 소수 금액이 실제 저장되기 전에만 무손실**(저장 후 `round()::bigint`로 소수부 소실). ※ 프론트 `Math.round`·RPC `round()`는 유지(제거는 별건). **2026-08-27 실측으로 3컬럼 numeric 재확인.**
- **사용 점검 화면 신설(0080 apply #249 `2fa94b2` + 화면 #250 `0ebd98e`, 2026-08-27)** — **목적: 유효기한이 임박하기 *전에* 장기 미사용 약품을 미리 판단**(폐기 손실 선제 차단). **구조: `drugs.last_used_date`/`dept` 재사용 — 신규 컬럼 0** + 이력 테이블 `drug_idle_reviews`(0080). **상태 4종**: 관찰 · 중지 · 보유유지 · 해제. **현재 상태 판정 = `drug_code`별 `(reviewed_at DESC, created_at DESC)` 최신 1행 — 화면 계산(RPC 아님)**. **구간**: `settings.idleBands` 기본 `[90,180,365]` — **조정 UI는 미구현**. **인라인 편집**: `saveField`(App.jsx:1808) 패턴 — ★ DrugEditModal의 `ud` **「빈칸=기존값 유지」 규약([[함정 #12]])과는 별개 경로로, 빈칸이 clear로 저장**된다(혼동 주의). 설계 판단 3건은 **§5-17**(되돌리지 말 것). 미결 1건은 §2 [판단 대기].
- **`change`(약품변경) ROUTES 누락 복구(PR #248 `fcc74b4`, 2026-08-27)** — 렌더 스위치에는 있으나 **`ROUTES` 배열에 등록되지 않아** 해시 딥링크·**새로고침 복원**이 불가했던 결함. ★ **신규 화면 추가 시 `ROUTES` 등록을 반드시 확인할 것**(렌더 스위치만 추가하면 화면은 뜨지만 URL 복원이 죽는다 — 조용한 결함).
- **재고 보정 total_amount 캐스트 실패 해결(PR #242 `40ca3a1` + 0078 apply, #243, 2026-08-27)** — **증상**: 재고 보정 시 `invalid input syntax for type bigint`(22P02), **소수 재고 44종에서만** 발생(정수 재고는 정상). **원인**: 차이수량(소수)×단가 = 소수 금액을 `transactions.total_amount`(**bigint**)에 넣으려다 실패([[함정 #15]]). ★ **마감 가드·0055 가드와 무관** — 가드는 값 캐스트 **이후** 단계이고, 마감 가드는 「거래 날짜의 월 + 스냅샷 존재」로만 판정(마감 실행 시각 미참조)하므로 익월에 전월 마감해도 당월 거래는 잠기지 않음(2026-08 미마감·정상 흐름 확인). **조치**: 프론트 2곳 `Math.round`(보정 App.jsx:829·TxForm 2867, PR #242) + `0078_bulk_stock_adjust_round_amount.sql`로 RPC `round()` 명시(apply 2026-08-27). ※ **RPC는 원래 실패 경로가 아니었음**(`numeric×numeric→bigint` 대입 캐스트라 자동 반올림) — 0078은 **명시화 개선**. apply 후 재검증 7항목 통과(소수 diff total_amount 정수·quantity 소수 보존·정수 회귀·마감월 차단·0055 가드·정본 885,285,628/106,365,758)·검증분 전량 ROLLBACK. 근본안(금액 컬럼 numeric)은 **0079로 완료**(위 항목 참조) — ~~§2 [중기·금액 타입 트랙]~~ 트랙 종료(2026-08-27).

---

## 2. 미완료 / 잔여 (2026-08-07)

### 신규 데이터 정합·무결성 트랙
| 항목 | 내용 |
|---|---|
| ~~**0055 가드 미적용**~~ → **0055 가드 적용 완료(2026-08-23)** | 구 기재(오류): apply 완료. 트리거 `trg_guard_drugs_qty_direct` + 세션변수 `app.qty_via_tx` 예외(정당 경로 = `apply_tx_to_inventory`·`revert_tx_from_inventory` 둘뿐, IS DISTINCT FROM일 때만 차단·INSERT 미차단·위반 시 23514). 적용 후 0056 감사 `path`가 '거래(trigger)'로 구분 기록. |
| ~~**372건 재고 불일치**~~ → **정합 정상(오류 아님)** | 구 기재(오류): 판정식이 로드 opening(이월) 잔고를 미포함해 생긴 차이일 뿐 데이터 손상이 아님 — 과소(부족) 0건, 순액 ≈₩92.3M ≈ 현재고 총액. **실사 기준 = current_qty vs 실물**이며 임의 보정 금지. 실제 거래 외 변동은 `drug_qty_audit` 110약품/139건(08-10~21)으로 확인되고, 0055 가드로 이후 차단됨. |
| ~~**07-18 단가 791건 미규명**~~ → **경로 규명 종결(2026-08-23)** | 구 기재(오류): `drug_price_history`의 `source` 컬럼·일괄 변경 흔적으로 07-18 단가 일괄 이벤트 확인. 재고 수량 불일치와는 인과 없음(겹침 141건 = 단순 교집합). |
| **item_count 정의 불일치** | 6월(엑셀결재본)=531 종수 vs 7월(runClose)=1,117 `rows.length`(전 약품). 정의 통일 필요. |
| **8월 잔차분 기준선** | 189,019 (7월 보정 (1,899,271)=조정 (1,710,252)+재평가 (189,019)). |
| **ABC/XYZ 분류** | 도매사 매핑(32/1,117) 전제 무효 → 설계 재검토 필요. |
| **monthly_snapshots 마감 시각 미기록**(감사성·낮음) ← **[중기·금액 타입 트랙]에서 이관(2026-08-27)** | `created_at` 등 타임스탬프 컬럼이 없어 「언제 마감했는지」 DB로 추적 불가. 2026-08 진단에서 7월 마감 시각을 확인할 수 없어 8월 거래와의 선후 판정이 막혔음(결론은 무영향). **승격 조건: 결재 이력과 대조할 필요가 생기면 승격.** 조치는 스키마 컬럼 추가. <br>**이관 사유**: 금액 타입 트랙은 「1)이 2)·3)을 함께 해소한다」는 묶음 전제로 만들어졌고 **0079로 그 전제가 소진**됨. 본 항목은 **마감 산출물의 정의·추적성 문제**라 정합 트랙이 제자리(`item_count` 정의 불일치와 동류). |

### 기존 잔여
- ~~0003 tenant_id NOT NULL(4테이블 nullable)~~ ← 구 기재(오류): 해당 마이그는 **0059+0060**이며 **적용 완료**(0003 파일 없음) · ~~0058 junk 테이블 DROP(apply 대기)~~ ← 구 기재(오류): **0058 적용 완료(PR #176)** · Portone V2 결제(미착수) · member role 격리·삭제거부 실증(owner 1명뿐, 계정 부재) · 발주 승인 워크플로(부분) · Vitest+RTL(인프라+1건) · App.jsx 분해 · 네이버 외부 콘솔(확인 불가).

> **잔여 백로그 (2026-08-23 재정렬)** — 위 §2의 완료 항목(0055 가드·07-18 단가·372건)은 해소됨. 미결 백로그 4분류 + 실사용 확인 1건으로 관리:
>
> **[단기 · 시점 게이트] 9월 마감 후 3건**
> 1. **manual→auto 전환**(usage_source=manual 691건) — 아래 「manual 해제 검토」 절차 및 **「실행 트리거」 트리거 1 참조**.
> 2. **ATC 순위 2항 가중식** 도입.
> 3. **ATC 제안 재산출·재배치**.
>
> **[중기 · 레이아웃 트랙] 3건** — 모두 `#root`·화면 폭 관련이라 함께 검토 시 회귀 검증 1회로 종결(개별 착수 시 같은 화면을 3번 검증하게 됨).
> - **① #root 폭 1126px 고정** — index.css `#root{width:1126px}`로 앱 전체가 중앙 컬럼에 갇힘(1920 모니터 좌우 여백 ~41%). 영향: 15컬럼 표가 1084px 안에서 가로 스크롤·발주관리 도매사 입력칸 6개 압축. 판정: **치명 결함 아님**(데이터·기능 정상, 공간 활용 문제). 착수 전 카드·도넛·모달·인쇄가 넓은 폭에서 안 무너지는지 **Feasibility Study** 필요.
> - **② #root text-align:center 상속 ★재발성** — index.css:61 `#root{text-align:center}`가 전 화면 상속원 → `textAlign` 미지정 셀은 전부 가운데 정렬. 사례(**세 번 발생**): #230(약품변경 「변경될 약품명」)·#235(발주관리 도매사·담당자·전화·메모 4열)·#238(발주관리 제목·중앙 렌더). 새 표를 만들 때마다 재발(발견 시마다 개별 수정 중). 대안: **(가)** 현행=발견 시 셀에 textAlign 명시(반복·누락 소지) / **(나)** #root를 left로 변경(전 화면 영향, 의도적 가운데 정렬 요소=카드·도넛·대시보드 숫자까지 흔들림·위험) / **(다)** 표 컨테이너에서만 left 재정의(영향 범위 통제·**권장**). 착수 시 (다) 기준 Feasibility Study.
> - **③ 모바일 최적화** — 선행 진단 완료(2026-08-23, 작업목록 6건·확인 3건), 착수 보류. (기존 [중기·착수보류]에서 이관)
>
> ~~**[중기 · 금액 타입 트랙] 3건**~~ → **트랙 종료 (2026-08-27)** — 3건 중 **2건 해소, 1건 이관**으로 잔여 0. 트랙은 「1)이 2)·3)을 함께 해소한다」는 묶음 전제로 만들어졌고 **0079로 그 전제가 소진**되어 닫음.
> - ✅ **금액 컬럼 numeric 전환** → 해소(**0079 apply · PR #246 · `de53ffc`**). 상세 §1 완료 이력 · [[함정 #15]].
> - ✅ **프론트/DB 반올림 방식 상이** → 해소(근본 원인인 금액 bigint 강제 소멸). ⚠ **편차 1원의 잔존 조건과 「제거를 권하지 않는 근거」는 [[함정 #15]]의 조건부 메모로 이관** — 제거하지 말 것.
> - ➡️ **monthly_snapshots 마감 시각 미기록** → **§2 [신규 데이터 정합·무결성 트랙]으로 이관**(승격 조건 유지). 마감 산출물의 정의·추적성 문제라 그쪽이 제자리.
>
> **[장기 · SaaS 확장] 2건**
> 4. **member role 격리·삭제거부 실증 테스트** — owner 1명뿐·계정 부재로 미실증. (※ `profiles_update_own` 자가승격 취약점은 **0077 가드로 해소** — §1 완료 이력 참조.)
>    - ★ **0005 재적용을 여기서 함께 처리 (2026-08-27 추가)** — `profiles_update_admin` 정책 부재로 **관리자가 타 사용자 profile을 수정할 수 없다**(§0 정정 참조). **사용자 1명 상태에서는 무해**하므로 단독 착수 불요이나, **member 계정을 만드는 순간 필요해진다** → 격리 실증과 **같은 시점에 처리**하는 것이 검증 1회로 끝난다. 보안 영향 없음(0077 가드 정상). ※ 지금 apply하지 않음.
> 5. **결제 서버 아키텍처 결정 + Portone V2** — 빌링키 + Webhook HMAC-SHA256 검증. 미착수.
>
> **[네이버 계정 생성 제한] 1건 (2단계 · 착수 보류)**
> - GoTrue `disable_signup=true`이나 `netlify/functions/auth-naver-callback.js`가 service_role `admin.auth.admin.createUser({email_confirm:true})`로 **disable_signup 우회** — 허용목록·도메인 제한 없이 어떤 네이버 이메일이든 즉시 계정 생성(생성 계정 role='user'·tenant_members 미편입). **현재 위험 낮음**: 0077로 자가승격이 막혀 테넌트 데이터·타 profiles 접근 불가. 단 profiles 무제한 누적·member 온보딩 시 정체불명 계정 혼입 우려. 조치안: **(가)** 허용목록 env / **(나)** `createUser` 제거·기존 사용자만 로그인 / **(다)** 초대 코드 — **(나) 권장**(사용자 1명·신규는 admin 직접 생성이면 자동 생성 불필요). ⚠ 착수 시 로그인 경로 변경이므로 **프리뷰에서 네이버 로그인 실동작 확인 후 병합**.
>
> **[판단 대기] 3건**
> - **드롭다운(ColMenu 필터) 항목 컬러 강조** — 사용자 육안 판단 대기(구현 전).
> - **약품 클릭 시 첫 클릭 무반응 (간헐)** — **진단 2026-08-28 · main `39eb751` · 100% 읽기 전용(코드 정적 분석)**
>   - **증상**: 약품명 클릭 → 첫 클릭 무반응, **두 번째에 편집 모달**이 열림. 오류 메시지·콘솔 에러 없음. **약품목록에서 체감 증폭**(가장 자주 쓰는 화면).
>   - ⚠️ **아래 1~3순위는 2026-08-28 전부 기각됐다** — 조사 경위 보존용으로만 남긴다. **결론은 이 항목 말미의 「진단 경과」를 볼 것.**
>   - ~~**1순위**~~ ❌ **기각 · 오버레이 히트테스트 (관측)** — GNB 드롭다운(**z 950**)·ColMenu 헤더 필터 메뉴(**z 9999**)가 **표 위를 덮는다**([[함정 #18]]). 덮인 자리를 클릭하면 오버레이가 그 클릭을 받고, **두 번째는 닫힌 뒤라 정상 동작** → 「첫 번째만 안 되고 두 번째는 된다」는 증상과 **정확히 일치**. 약품목록은 **ATC 도넛 3종·필터 칩 4줄**로 표가 아래로 밀려 **덮이는 면적이 가장 크다**.
>   - ~~**2순위**~~ ❌ **기각 · 렌더 지연 체감 (추론)** — 클릭이 **루트 상태**를 바꿔 전 트리가 리렌더되고, 그 안에서 1,114건 필터·정렬 + 도넛 3종 집계 + **대형 편집 모달 마운트**가 **모두 동기**로 돈다. `useMemo`·`useCallback` **사용 0회**(5,182줄, [[함정 #19]]). **누름 피드백·로딩 표시가 없어** 「안 눌림」으로 읽힌다(클릭은 처리됐고 화면만 늦은 경우).
>   - ~~**3순위**~~ ❌ **기각 · 클릭 중 행 이동·제거 (추론)** — realtime 구독(0024)이 `drugs`·`transactions`·`inventory_stock` **전 이벤트**에 반응해 400ms 후 **전량 재조회**. `mousedown`~`mouseup` 사이에 행이 바뀌면 **`click` 자체가 발생하지 않는다**.
>   - ※ **리스너 소비설은 배제** — document/window 리스너를 전수 확인한 결과 **실제 이벤트 소비형은 훅A(`useDraggableModal`)의 capture 단계 `click` 1건뿐**이고, 그것도 **모달을 드래그로 움직인 직후 1회**로 한정된다(`once` + 타이머 제거). 나머지 바깥클릭 리스너(GnbNav·ColMenu·ColumnSelector·전역검색·DonutSetMenu·날짜입력·엑셀 드롭다운)는 **전부 「닫기만」 하는 비소비형**이며 `preventDefault`·`stopPropagation`을 하지 않는다. **cleanup 누락도 발견되지 않았다.** → **리스너가 클릭을 삼키는 것이 아니라 히트테스트 문제.**
>   - **판정: 공통 컴포넌트 문제** — 클릭→모달 패턴·ColMenu·GNB·루트 상태 갱신 구조가 **13개 화면에서 사실상 동일**하고, 약품목록에만 있는 요소 중 **클릭을 직접 소비하는 것은 없다**. 약품목록은 **체감 증폭 지점**일 뿐 → **다른 화면에서도 낮은 빈도로 발생 중일 가능성.**
>   - ★ **재현 절차 3가지 — 아직 실행되지 않음(미검증)**. 진단은 **코드 정적 분석만**이고 **브라우저 재현은 시도하지 않았다**(Claude Code 헤드리스 — §6 인쇄 협업 프로토콜과 동일 사유). 아래는 사용자가 직접 시험할 절차다.
>     1. **GNB 메뉴에 마우스를 올려 드롭다운을 편 채** 표 **맨 윗줄** 약품명 클릭 → 안 열리면 **1순위 확정**. 이어서 스크롤해 **중간 행** 클릭 → 한 번에 열리는지 **대조**.
>     2. 드롭다운을 **모두 닫고** 중간 행을 **한 번만** 클릭 후 **2초 대기** → 늦게라도 열리면 **2순위(지연)**. **검색어 입력 직후 1초 이내** 클릭 시 더 잦은지도 확인(검색에 디바운스 없음).
>     3. **재고현황·유효기한**(도넛 없음)에서 ①②를 반복 → **재현되면 공통**, 약품목록에서만이면 **레이아웃 요인**. ※ **약품코드**(360° 모달)와 **약품명**(편집 모달) 중 어느 쪽인지도 함께 확인하면 경로가 좁혀진다.
>   - **[사용자 관측] 2026-08-28 — 미재현.** 사용자가 약품목록에서 약품명 클릭을 확인했으나 **그 시점에는 정상 동작**했다. ※ **위 절차 ①②③을 그대로 따른 것은 아니며, 일상 사용 중 확인한 결과**다(따라서 ①②③은 여전히 미검증). → **「간헐적 발생」이라는 성격을 뒷받침하는 관측** — 항상 나는 결함이 아니므로, 이후 조사는 **「언제 나는가」(선행 조건)를 좁히는 방향**이어야 한다. ★ 이 관측은 위의 **코드 정적 분석과 별개 항목**이다(둘 다 유지).
>
>   ### ★ 진단 경과 (2026-08-28 갱신) — 위 1~3순위를 포함해 **가설 5건 전부 기각**
>
>   **★ 기각된 가설 5건** — 같은 가설을 다시 세우지 말 것.
>
>   | # | 가설 | 기각 근거 |
>   |---|---|---|
>   | ① | **오버레이 히트테스트**(GNB z950·ColMenu z9999가 표를 덮음) | **GNB를 거치지 않은 동선**(즐겨찾기 직접 진입)에서도 재현 |
>   | ② | **리렌더·행 교체** | `dsearch` 불변 → `filtered`·행 수·key 동일 → **DOM 재사용**. 값이 같아 **React가 리렌더 자체를 bail out** |
>   | ③ | **검색 디바운스 250ms**(재계산을 클릭 뒤로 미룸) | **PR #257**로 구현 → **재현**. 실시간 검색 회귀만 유발(조합 게이트는 별도 제거) |
>   | ④ | **백드롭 즉시 닫힘**(DrugEditModal `onClick={onClose}`) | 클릭 시 **화면 어두워짐·번쩍임 없음**(사용자 관측) |
>   | ⑤ | **도넛 집계 비용**(원본 1,114건 4회 순회) | **PR #258** `useMemo` 캐시 → **재현** |
>
>   ※ **정적 분석은 한계에 도달했다.** 코드에서 더 짜낼 정보가 없으므로, 다음 단계는 **실측**이다.
>
>   **★ 확정된 조건 (사용자 실측 · 모두 운영 main 기준)**
>
>   | 조건 | 결과 |
>   |---|---|
>   | **한글 검색 + Enter 없이 클릭** | ★ **무반응** |
>   | 클릭 전 **2초 대기** | 정상 |
>   | **영문 코드** 검색 | 정상 |
>   | **Enter** 친 뒤 클릭 | 정상 |
>   | **재고현황**(도넛 없음) | 정상 |
>   | 프리뷰 #257(디바운스) | ★ **도넛 오선택 형태로 변형** — 「혈액 및 조혈기관」 자동 선택 = **좌표 이동 시사** |
>
>   **★ 다음 단계 — DevTools 이벤트 계측** (정적 분석 대체)
>
>   - ★ **운영(main)에서 실행할 것** — PR #257·#258 변수를 배제해야 한다.
>   - **기록 대상**: 이벤트 순서(`compositionend`·`input`·`mousedown`·`mouseup`·`click`·`focusout`·`scroll`) · **클릭 좌표** · 각 시점의 `document.elementFromPoint` · **`scrollY`** · **LONGTASK**(재계산 점유 여부).
>   - ※ **계측 스크립트 본문은 이 문서에 싣지 않는다**(대화 이력 참조). 콘솔 붙여넣기용 읽기 전용 스니펫.
>
>   | 관측 결과 | 판정 | 조치 방향 |
>   |---|---|---|
>   | `click` 없음 + mousedown/mouseup의 `under` **동일** | **브라우저·IME가 `click`을 생성하지 않음**(앱 무관) | `onMouseDown` 전환이 사실상 유일 해법 |
>   | `click` `target`이 다름 + **`scrollY` 차이** | **좌표 이동** — 커서 밑 요소가 바뀜 | 그 이동원을 직접 제거 |
>   | mousedown~mouseup 사이 **LONGTASK** | **재계산이 클릭 창을 점유** | Performance 탭 Call Tree로 함수 특정 후 표적 최적화 |
>   | `click` `target`이 `TD`인데 모달 미열림 | **React 쪽 문제**(핸들러 도달 후 실패) | 전혀 다른 방향으로 재조사 |
>
>   **조치 후보**
>   - **(가) 시각 피드백 추가** — 클릭 즉시 행 하이라이트·로딩 표시. **원인과 무관하게 이득**(지연이면 해결, 아니어도 오작동 인지가 빨라짐). 규모 **반나절**. ★ 낮은 위험 · 계측과 병행 가능.
>   - **(나) 근본 조치** — ★ **계측 결과로 원인이 확정된 뒤에만.** 위 판정표의 네 갈래는 **조치가 서로 완전히 다르다.**
>   - ⚠️ **교훈**: ③·⑤는 계측 없이 가설만으로 구현했다가 **둘 다 재현**했고, ③은 증상의 모양만 바꿨다(무반응 → 도넛 오선택). **원인 확정 전에는 착수하지 말 것.**
>
>   **관련 PR (2026-08-28 기준 · 전부 OPEN · 병합 판단 대기)**
>
>   | PR | 내용 | 이 버그와의 관계 |
>   |---|---|---|
>   | **#257** `feat/druglist-search-debounce` | 검색 디바운스 250ms | ★ **버그 수정 실패(가설 ③ 기각).** 재계산 횟수를 자모 11회→1회로 줄이는 값은 남음 → **성능 개선으로만 판단** |
>   | **#258** `feat/druglist-donut-memo` | 도넛 `useMemo` + 표 `min-height` 948px | ★ **버그 수정 실패(가설 ⑤ 기각).** `useMemo`는 성능 개선으로 유효하나, **`min-height`는 근거가 사라져 되돌리는 것이 맞음** |
>   | **#254** `feat/idle-reason-modal` | 사용 점검 사유 모달 | **이 버그와 무관** · 프리뷰 확인 대기 |
>
>   ※ #257·#258은 **버그 수정으로는 병합하지 말 것.** 성능 개선으로 취할지는 별도 판단(폐기 여부 포함) — **사용자 결정 사항.**
> - **사용 점검 — 상태·메모 중복 이력** (2026-08-27 등록) — **상태 변경(INSERT) 후 메모 입력(INSERT)이 별도 행으로 쌓여 같은 판단이 2행**이 된다. 실제 **5개 코드**(STRSM·RSTSEDP·URO-Q·NBT150·RBNN3·7ATV2)에서 발생, 각각 15~30초 간격. **스키마상 정상**(§5-17 UNIQUE 미부여 = 이력 누적이 의도)이며 **결함이 아니라 화면 UX가 유도한 결과**. 조치안: **상태 변경 시 메모를 함께 받는 모달 → 1행으로 저장**. ⚠ **기존 15행은 실사용 데이터 — 삭제 금지**(정리 목적의 DELETE 포함).
>
> **[실사용 확인] 0055 가드** — 적용 완료(2026-08-23, PR #227). 별도 테스트 불요 · 평소 업무 중 자연 관찰.
> - **확인 방법**: ① 약품 신규 등록 → 정상 저장이면 정상 · ② 엑셀 업로드(차이 0) → 「조정 거래 0건」이면 정상 · ③ 엑셀 업로드(차이 있음) → 조정 거래 생성되면 정상.
> - **이상 신호(이 경우에만 보고)**: 위 경로에서 오류코드 **23514** 또는 「직접 수정 차단 — 재고는 거래로만 변경」 메시지가 뜨는 경우.
> - **배경**: 가드는 `drugs.current_qty` 직접 UPDATE만 차단. 정당 경로(`apply_tx_to_inventory`·`revert_tx_from_inventory`·`bulk_stock_adjust` RPC)는 세션변수 `app.qty_via_tx`로 통과하며, 월마감(runClose)은 current_qty를 변경하지 않아 무관.
> - **롤백**: 가드 트리거·함수 DROP + `apply_tx`/`revert_tx`를 0015 정의로 복원. Git 롤백 기준 `15df078`.
>
> **[실사용 확인] 0077 가드** — 적용 완료(2026-08-24, PR #249). profiles.role 자가변경 차단(23514).
> - **확인 방법(3건)**: ① 즐겨찾기 편집·저장 정상 · ② 약품목록 컬럼 설정 변경·적용 정상(각 확인 후 원복) · ③ **네이버 로그인 정상**(가드는 service_role 콜백을 통과시키므로 코드상 무영향, 실제 로그인으로 최종 확인).
> - **이상 시**: 전체 롤백보다 **원인 진단 후 표적 수정 우선**(취약점 재노출 방지). 롤백 SQL = `drop trigger trg_guard_profiles_role_direct on public.profiles;` + `drop function guard_profiles_role_direct();`.
>
> **[실사용 확인] drug_master 자동입력** — 병합 완료(2026-08-26, PR #240 `48b5787`). 별도 테스트 불요 · 평소 약품 등록 시 자연 관찰([[함정 #14]]).
> - **확인 방법**: ① 보험코드 입력 → ATC·첨가제·제조사 등 자동 채움 · ② 이어서 약품명 입력·Enter → ★ **채워진 값이 유지되면 정상**(결함 A 해소) · ③ 비보험 약품 → 품목기준코드로 자동 채움 · ④ 값이 비면 **[마스터 조회] 버튼**으로 재충전.
> - **이상 신호(이 경우에만 보고)**: 약품명 입력/Enter 후 자동 채움 값이 **사라지는 경우**(결함 A 재발 — `lookupApi` 리셋 완화가 되돌려졌다는 뜻).
> - **배경**: 조회 키 우선순위 = 보험코드 → `product_code`(1:1, 중복 시 미적용) → 약품명 ilike. 커버리지 68%→100%(비보험 포함). ※ drug_master의 동명 `standard_code` 컬럼은 다른 체계 — 조회에 **쓰지 말 것**(품목기준코드는 `product_code`). 채움 정책은 빈 값만(`_mergeDrugMaster`)·`lookupApi` 리셋 제거로 「기존 값 보호」 통일.

> **[실사용 확인] 사용 점검 화면** — 병합 완료(2026-08-27, #249 0080 apply + #250 화면 `0ebd98e`). 별도 테스트 불요 · 평소 사용 중 자연 관찰.
> - **확인 방법**: ① 사용 점검 화면 진입 → 장기 미사용 목록 표시 · ② 상태 지정(관찰/중지/보유유지/해제) → 저장 후 **최신 상태로 반영되면 정상** · ③ 새로고침·해시 딥링크로 화면 복원 정상(#248 ROUTES 등록 확인) · ④ 메모 입력·수정 정상.
> - **이상 신호(이 경우에만 보고)**: **`42501 permission denied`**(→ GRANT 문제, [[함정 #16]]) · 상태 저장 후 목록에 반영 안 됨 · 새로고침 시 화면이 초기 화면으로 튐(ROUTES 회귀).
> - **알려진 미결(보고 불요)**: 상태 1건이 **2행으로 쌓이는 현상** — 위 [판단 대기] 참조. **정상 동작이며 데이터 손상 아님.**
> - **현황(2026-08-27 실측)**: `drug_idle_reviews` **15행 / 약품 9종**(중지 13 · 해제 2 · 관찰 0 · 보유유지 0), `reviewed_at` 전부 2026-08-27, 메모 보유 6행. **실사용 데이터이므로 삭제·수정 금지.**

### 실행 트리거 (9월 마감·실사) — 기록만, 지금 실행 금지

**트리거 1 — 9월 마감 완료 후 (manual→auto 재대조)** ※ [단기 · 시점 게이트] ①과 연결
- **조건**: `transactions`가 실거래 3개월분 충족(현재 7·8월분뿐이라 롤링 3개월이 사실상 1개월).
- **동작**: recent_3m 저장값 vs 실거래 3개월합 재대조 → 판정 전이 시뮬레이션 → **건수 보고** → 극소량 품목 manual 유지 여부 분기 → **승인 후** 일괄 전환.
- **주의**: **승인 전 일괄 전환 금지.**

**트리거 2 — 9월 실사 당일 아침 (실사 대조표 산출)**
- **조건**: 실사 시작 전.
- **동작**: `node scripts/stocktake_report.mjs <출력경로>` (기본 = 재고 보유 약품만 · `--all` = 전량).
- **주의**: current_qty는 매일 변동 → **이전 산출물 재사용 금지, 반드시 당일 새로 산출.**
- **참고**: 「우선대조」 시트(08월 직접변동 110약품)부터 대조 권장. 실사 기준 = **current_qty vs 실물**(거래누적 대조 아님). 371건 「불일치」는 로드 opening이므로 **임의 보정 금지**.

### manual 해제 검토 (예정: 2026-09 마감 후)

**현황 (2026-08-07 실측)**
- `usage_source='manual'` **691건**
- recent_3m 저장값 ≠ 실거래 3개월합 = **405건 (58.6%)**
- 방향: 대부분 저장값 > 실거래 (**3~4배**). 예) DPHALEYS5 저장 36,805 vs 실거래 9,925
- 원인: `transactions`가 **7월분(730행)뿐**이라 "실거래 3개월"이 실제로는 1개월분

**해제 금지 사유**
- 지금 해제 시 `monthly_avg`·`safety_stock`이 3~4배 축소 → **결품 위험**
- 극소량 품목 4건(5D/W10·OMCKECM·SKYZST1·VTLEV)은 `round(1/3)=0`으로 safety=0 → **'기준미설정' 전락, 판정 상실**
- SKYZST1은 현재 '긴급'인데 해제 시 알림에서 사라짐

**안전성 확인 (2026-08-07)**
- 실거래 > 저장 위험군 **40건 전수 점검**
- 현재 '정상' → 재산정 시 '긴급/주문필요' 전이 = **0건**
- 전량 현재고 ≫ 재산정 안전재고 (여유 2~113배)

**해제 절차 (9월 마감 후)**
1. recent_3m 저장값 vs 실거래 3개월합 재대조
2. 차이 축소 확인 (3~4배 → 1배 근접 예상)
3. 해제 시 판정 전이 시뮬레이션 (긴급·주문필요 변동 건수)
4. 극소량 품목 manual 유지 여부 별도 판단
5. 승인 후 `usage_source` 일괄 auto 전환

> 8월 운영은 안전: 「출고 자동집계」는 `p_overwrite_manual:false`라 manual 691건 미변경. 관련 메모리 `manual-usage-lock`.

---

## 3. 핵심 산식 (회귀 방지 — 매우 중요)

### 단가/금액 (CLAUDE.md 철칙)
- **`purchase_price` = 구입단가(개당)** — **모든 금액 계산의 유일한 기준**.
- `price_unit` = 통당단가(포장 단위, 표시용) — **금액 계산에 쓰지 말 것**(정제류 ~100배 부풀림).
- `edi_price` = 보험약가(참고/이력용).
- `재고금액 = Σ(current_qty × purchase_price)` · `거래금액 = quantity × purchase_price`.
- 검증 기준 — ★ **두 값의 성격이 다르다. 반드시 기준일과 함께 읽을 것**:
  - **현재고 총액 = 변동값** — `Σ(current_qty × purchase_price)` = **123,278,481.969** (**2026-08-27 실측**). ~~106,365,758(=7월 기말)~~ ← **구 기재**: 7월 기말은 마감 시점 스냅샷이지 현재고가 아니며, 이후 8월 거래·보정이 반영돼 값이 달라졌다. **매일 변하므로 판단 전 재측정하고, 새로 적을 때 측정일을 함께 남길 것.**
  - **정본(마감) = 고정값** — 1~7월 합 **885,285,628.424** · 7월 기말 **106,365,758.469**(아래 무오차 기준선). 이 값이 바뀌면 회귀를 의심할 것.

### 정본(사이드카) vs 스냅샷
- **`monthly_report_totals`** = 정본(월 총계 단일행, 결재본). 화면·인쇄·엑셀 요약은 마감월에서 **이것**을 읽음.
- `monthly_snapshots` = per-item 스냅샷(약품별 행). 명세 시트·per-item 표시용.
- 마감월 표시: `sideTot ? Number(sideTot.X) : (live ? live.X : tot.X)` 패턴(3경로 동일).

### 항등식 · 보정 구성
- `전월재고 + 입고 − 출고 − 폐기 − 반품 + 보정 = 기말`. **오차 0** by construction.
- 보정값 = `liveAudit`(잔차 plug) = `기말 − (전월+입−출−폐−반)`.
- 보정 구성 = **조정거래분(Σ조정×pp) + 재평가(잔차)**. 재평가 = `round(보정값) − round(조정거래분)`(표시용 통일 → 보조 2행 합=메인).

### 1~7월 정본 actual_closing (무오차 기준선)
`155,101,116 / 149,997,617 / 129,432,154 / 124,104,134 / 113,063,588 / 107,354,133 / 106,365,758`

### 2026-07 마감 실측 (시스템 runClose 1호)
| 항목 | 값 |
|---|---|
| 전월재고 | 107,354,133 |
| 입고 | 33,560,118 (286건) |
| 출고 | 32,002,763 (400건) |
| 폐기 | 507,459 (9건) |
| 반품 | 139,000 (3건) |
| 보정 | (1,899,271) = 실사 조정 32건 (1,710,252) + 재고 재평가 (189,019) |
| 기말 | 106,365,758 · **항등식 오차 0** |

---

## 4. 데이터 구조 · 트리거

- **drugs**: 마스터(current_qty, purchase_price, status 사용/중지/휴면, expiry_date, safety_stock…). tenant 격리(RLS). `trg_set_tenant_id`·`trg_enforce_price_edit_role`·`trg_log_drug_price_change`·(0056)`trg_drugs_moddatetime`·`trg_log_drugs_qty`. ~~(0055 미적용: 가드 트리거 없음.)~~ → **0055 적용 완료(2026-08-23): 가드 트리거 `trg_guard_drugs_qty_direct` 존재**(직접 current_qty UPDATE 차단, 예외 세션변수 `app.qty_via_tx`).
- **transactions**: 입고/출고/폐기/반품/조정. `type='조정'` quantity=목표−현재(부호). `trg_apply_tx_to_inventory`(AFTER INSERT)·`trg_revert_tx_from_inventory`(AFTER DELETE)가 inventory_stock+drugs.current_qty 동기. 마감월 가드 3종(0052/0054).
- **inventory_stock**: 재고 정본(numeric). **current_qty는 이 트리거들만 갱신**(직접 수정 금지 규약).
- **monthly_snapshots**: per-item 마감 스냅샷. 흐름 컬럼: total_in/out_qty·amount, total_disp/ret_qty(폐기·반품 **금액 컬럼 없음** → qty×pp 파생), opening/closing_qty·amount. (subtotal_qty/amount 레거시 미사용=0.)
- **monthly_report_totals**: 정본 총계 + item_count·in/out/disp/return_count·exp_* 버킷·audit_adjust·reason_note·source('엑셀결재본'|'시스템').
- **bulk_stock_adjust(p_items, p_date, p_reason)** RPC(0057): 신규→drugs INSERT(current_qty=0)+조정거래(목표), 기존→(목표−현재) 조정거래(0 skip). 단일 트랜잭션·마감월 검증→실패 시 전량 롤백. 반환 {ok,success,adjusted,failed[]}.

---

## 5. 함정 목록 (Pitfalls)

1. **price_unit로 금액 계산 금지** — purchase_price만. (2026-06 회귀: 월마감 1.13억→123억.)
2. **transaction_date는 DATE 타입**(시각·TZ 없음). timestamptz 리터럴 삽입 시 UTC 캐스팅으로 월 오귀속(테스트 인공물). 비교는 날짜 문자열로.
3. **엑셀 요약은 마감월에 정본을 읽어야 함** — `tot.*`(스냅샷/live)로 읽으면 어긋남. 6월 스냅샷 흐름=0(엑셀결재본 적재분)이라 tot.ia=0.
4. **명세 시트는 아직 `tableData=live?live.rows:snaps`** — 마감월에 live 잔존 시 현재 데이터 표시(마감월 소스 분기 코드 잔여 과제).
5. **폐기/반품 금액은 스냅샷에 없음** → qty×**현재 pp** 파생. 정본 disposal_amount/return_amount(마감시점 pp)와 단가 변동분만큼 차이 가능. 요약은 정본 사용이라 무영향.
6. **item_count 정의 2종**(엑셀결재본 종수 531 vs runClose rows.length 1,117) — 통일 전 혼용 주의.
7. **CLI/pg 연결 pooler 사용**: .env DATABASE_URL 직접 host(db.*.supabase.co)는 미해석 → `aws-*.pooler.supabase.com` 사용. `/postgre`→`/postgres` 오타 보정.
8. **App.jsx 편집**: CRLF + 한글 NFC/NFD. 다중행 `\n` 앵커는 CRLF에서 실패 → 단일행 앵커. 한글 라인은 Edit 도구 실패 잦음 → node 스크립트(count===1 가드).
9. **SheetJS 커뮤니티판(xlsx@0.18.5)**: 셀 스타일(굵게·배경)·freeze **쓰기 미지원**. `.z`(숫자서식)·`!cols`(폭)만 지원.
10. **재고 변경은 조정거래로만** — 직접 current_qty UPDATE 금지(~~0055 가드 예정~~ → **0055 가드 적용 완료 2026-08-23**, 위반 시 23514). 초기재고·엑셀 재고열도 RPC 경유.
11. **min-height:100vh + 인쇄 = 백지 페이지** — 인쇄에서 `vh`는 페이지 높이로 해석되므로 `min-height:100vh` 컨테이너가 **최소 1페이지를 예약**한다. `@media print`에서 해제하지 않으면 빈(잉여) 페이지가 생긴다. 본 코드베이스 **2회 발생**: 비상조제(과거 자체 해결 — `.ed-root`의 `@media print{min-height:0}`, 주석 명시)·ATC(PR #239에서 `#root`·`.cnc-app-root`까지 해제 확장). ※ 단 **ATC 중간 백지의 원인은 아니었음** — 그건 고정 46행 분할 + `page-break-after:always` 조합(측정으로 확정, PR #239 (d)로 단일 표 자동흐름 전환). **새 인쇄 화면 제작 시 조상 체인의 `vh` 단위부터 확인**할 것.
12. **수정 저장 `ud` 「빈칸 = 기존값 유지」 규약** (App.jsx:589 주석) — DrugEditModal **수정** 저장은 `_has(값)`이 참일 때만 `ud`에 병합하므로, **값을 지워도 payload에서 제외되어 기존 DB 값이 유지**된다(실수로 지운 값 보호·**의도된 설계·버그 아님**). 보호 대상 **13개**(빈값 가능하나 클리어 안 됨): text 10(ingredient_en·ingredient_kr·manufacturer·lot_no·insurance_code·efficacy·efficacy_class·specification·storage_location·atc_code) · textarea 1(memo) · date 1(expiry_date) · select 1(prescription_type, 분류 토글 해제 시 '' 가능). (+ drug_name은 필수라 사실상 클리어 안 함.) select 5종(category·narcotic_type·insurance_type·storage_method·compound_type)은 항상 값이 있어 무관. **★ 예외 1건 additive**(PR #243) — 사용자 요청으로 `f.additive || null` 빈값 병합 → 같은 모달에서 첨가제만 삭제되고 나머지는 유지되는 비대칭. **「비고를 못 지운다」류 문의 = 이 규약이 원인.** ※ **INSERT(`row`)·Drug360Modal `saveEx`는 `|| null` 패턴**이라 이 규약과 무관하게 빈값이 저장됨.
13. **모달 창 드래그 구현 3종 병존** (새 모달 작성 시 필독) — ⓐ **훅A `useDraggableModal`**(외부 파일 `useDraggableModal.jsx`·**8개 모달**): 경계 clamp(EDGE 40px)·resize 보정·backdrop click 삼킴 **전부 보유** → **신규 모달은 반드시 이것을 쓸 것**. ⓑ **훅B `useDragModal`**(App.jsx 로컬·**4개 모달**: ATC exclude/manage·Schedule day/event): 경계만 보유, resize 보정·backdrop 삼킴 없음. ⓒ **DrugEditModal 인라인**(1개): 경계는 PR #247로 보강, resize 보정·backdrop 삼킴 없음. ※ **셋 다 mouse 이벤트 전용 — 터치 미지원**(모바일 최적화 트랙에서 함께 다뤄야 함). ※ 3종 일원화는 **12개 모달 회귀 검증** 수반 → 별건([[모달 트랙]]).
14. **약품 등록 자동입력 = 「기존 값 보호」로 통일** (PR #240, 48b5787) — drug_master 자동입력의 두 경로가 이제 **빈 값만 채움**으로 일치.
    - **채움 정책 통일**: `_mergeDrugMaster`는 `prev.X || dm.X`(빈 값만) · `lookupApi`는 **진입부 리셋 제거**(기존 값 유지, 대입부의 `||` 가드로 빈 필드에만 대입). ※ 이전 `lookupApi`는 진입 즉시 **8개 필드**(`atc_code·standard_code·specification·total_qty·packaging·ingredient_kr/en·manufacturer·insurance_price`)를 **무조건 `''` 리셋**해, drug_master가 채운 값과 사용자 입력이 함께 소실됐음. **ATC·첨가제는 API가 제공하지 않아 재충전조차 불가**였음(결함 A). ※ `ud` 「빈칸=기존값 유지」 규약([[함정 #12]])과 같은 취지 — **자동입력 전반이 「기존 값 보호」로 통일**됨.
    - **drug_master 조회 키 우선순위**: ① `insurance_code`(≥8자) → ② `product_code`(≥8자, **`.limit(2)` + `length===1`일 때만**) → ③ `drug_name` ilike 1:1. ★ **drug_master에 동명의 `standard_code` 컬럼이 별도로 있으나 다른 체계 — 조회에 쓰지 말 것. 품목기준코드는 `product_code`.** ※ 앱 폼의 `standard_code` 필드 ↔ drug_master.`product_code` 매핑(9자리 숫자문자열·동일 체계). ※ `product_code` **중복 1,110건** → **1:1 가드 필수**. ※ 커버리지: 보험코드만 **68% → product_code 추가로 100%**(product_code only 10,362행·**비보험 약품 포함**).
    - **재조회는 [마스터 조회] 버튼으로**(API 조회 버튼 옆·기존 색 재사용): `useEffect [insurance_code]`는 **값이 같으면 재실행되지 않아** 한 번 비워지면 자동 복구 불가(결함 B) → **명시 트리거** 신설. ⚠ **「자동 발동(effect) + 의존성 값 불변 시 미재실행」 구조는 다른 곳에서도 재현될 수 있으니 주의** — 복구가 필요한 흐름은 값 변화가 아닌 명시 트리거로 설계할 것.
15. **수량은 numeric·금액은 bigint — 타입 비대칭 + 캐스트 경로별 거동 상이** ★ (PR #242·0078) — 소수 금액 삽입이 **경로에 따라** 실패하거나 조용히 반올림된다.
    - **수량 계열(numeric·소수 허용)**: `drugs.current_qty`·`inventory_stock.current_qty`·`transactions.quantity`·`monthly_snapshots.*_qty`. ※ **0012·0013·0015**에서 numeric 전환됨.
    - **금액 계열(bigint·소수 불가)**: `transactions.total_amount`·`drugs.current_amount`·`inventory_stock.current_amount`. ※ **미전환** — 소수 금액을 넣으면 경로에 따라 실패 또는 암묵 반올림.
    - **★ 캐스트 경로별 거동 차이(진단 시 혼동 주의)**: **프론트 직접 insert** → PostgREST가 JS 소수를 문자열로 전달 → `'-781091.5'::bigint` = **22P02 오류**(실패). **RPC(SQL 내부)** → `numeric × numeric → bigint` **대입 캐스트** = **자동 반올림·무오류**. → **같은 값이라도 프론트 경로만 실패**한다(RPC는 조용히 반올림). ADLT 보정 오류가 프론트 경로여서 발생(§1 완료 이력 참조).
    - **현재 조치 = 계산 시점 반올림**: 프론트 2곳 `Math.round`(PR #242) + RPC `round()`(0078). ⚠ **프론트 `Math.round`(half→+∞)와 DB `round`(half away from zero)가 음수 half에서 1원 차이** — 실용상 무해하나 통일 안 됨(근본안=금액 numeric 전환은 **0079로 완료**, 아래 갱신·조건부 메모 참조).
    - **소수 재고 현황**: `drugs.current_qty` 소수 **44종**(오르필시럽 7724.1·페니라민 2781.5·ADLT 2645.5 등). **반알(0.5정) 처방 사용량이 numeric quantity로 유입돼 생성된 정상 데이터.** ★ **수량을 정수로 강제하면 반알 처방이 불가능해지므로 금지** — 해결은 금액 컬럼을 numeric으로 올리는 방향(수량 낮추기 아님).
    - ✅ **[2026-08-27 갱신] 금액 3컬럼 numeric 전환 완료(0079 apply · PR #246 `de53ffc`)** — 위 「금액 계열=bigint·미전환」은 **구 기재**. `transactions.total_amount`·`drugs.current_amount`·`inventory_stock.current_amount` 전부 **numeric**(실측 확인). **22P02 캐스트 실패는 소멸**하고 수량↔금액 타입 대칭 달성. ※ `unit_price`·`price_unit`·`edi_price`는 여전히 integer(0079 범위 밖).
    - ⚠️ **[조건부 메모] 1원 편차는 아직 잔존한다 — 반올림 코드를 제거하지 말 것** (2026-08-27, [중기·금액 타입 트랙] 종료 시 이관) — 위 갱신의 「1원 편차 소멸」은 **부정확했다**. 0079가 없앤 것은 **타입 제약(bigint 강제)**이지 편차 자체가 아니다. **프론트 `Math.round`(App.jsx:829·TxForm 2867)와 RPC `round()`(0078)가 그대로 남아 있는 한 편차는 지금도 재현된다.**
      - **재현 조건**: 음수이면서 소수부가 정확히 `.5`인 금액. 실제 예 `-781091.5` → 프론트 `-781091` / RPC `-781092`.
      - **★ 제거는 「부채 청산」이 아니라 정책 결정이다.** 편차를 없애려고 반올림을 걷어내면:
        - ⓐ **소수 금액이 실제로 저장되기 시작** → **0079 롤백의 무손실성이 사라진다**(저장 후 되돌리면 `round()::bigint`로 소수부 소실 — §1 완료 이력 경고).
        - ⓑ **화면·엑셀에 소수점 금액이 노출**된다(금액을 정수로 유지하는 현행이 실무상 바람직할 수 있음).
      - → **「제거」를 기본값으로 두지 말 것. 소수 금액을 실제로 쓰기로 결정할 때만 재검토한다.** 그 전까지 편차 1원은 **수용**한다(별건 백로그로 세우지 않음).
16. **신규 테이블에 GRANT 가 자동 부여되지 않음** ★ (2026-08-27 확인, 0080 사례) — 이 DB는 **default privilege로 `authenticated`에 권한을 주지 않는다.** 테이블을 만들기만 하면 RLS 정책이 아무리 올바라도 접근이 막힌다.
    - **신규 운영 테이블 생성 시 반드시 명시할 것**:
      ```sql
      grant select, insert, update, delete
        on public.<table> to authenticated;
      ```
    - **누락 시 증상**: dryrun·실사용에서 **`42501 permission denied`**. RLS 정책 오류로 오진하기 쉬우니 **GRANT부터 확인**할 것.
    - **실제 사례**: `0080_drug_idle_reviews` **첫 dryrun 실패**가 정확히 이 원인이었음(정책 4종은 정상이었으나 GRANT 부재).
    - ※ **`anon`에는 부여하지 않는다** — anon은 `current_tenant_ids()`가 비어 RLS로 전량 차단되므로 **기능 차이 0**이고, 0076 최소권한 취지에 맞다. (구 모델 `drug_change_plans`는 anon CRUD를 보유하나 이는 답습하지 말 것.)
17. **`drug_idle_reviews` 설계 판단 3건 — 「빠진 것」이 아니라 「의도된 것」** ★ (0080, 2026-08-27) — 참고 모델 `drug_change_plans`(0038~0040)를 복제하되 **3가지를 의도적으로 뺐다. 되돌리지 말 것.**

    | 뺀 것 | 이유 |
    |---|---|
    | **UNIQUE 미부여**(PK만) | 모델의 `(tenant, code, base_date)`를 답습하면 `(tenant_id, drug_code, reviewed_at)`이 되어 **같은 날 재판단이 제약 위반으로 막힌다** — 이력 누적이라는 테이블 목적과 정면 충돌. 최신 상태는 `reviewed_at DESC, created_at DESC`로 구한다. |
    | **status CHECK 미부여** | 모델 `plan_status`(자유 select)와 일관. **상태값 확장 시 마이그레이션이 불필요**해진다. 현재 값 4종: 관찰·중지·보유유지·해제. |
    | **anon GRANT 미부여** | anon은 RLS로 전량 차단되어 **기능 차이 0**. 0076 최소권한 개선(§5-16). |

    ※ `tenant_id`는 모델(nullable)과 달리 **NOT NULL** — `trg_set_tenant_id`가 채운 뒤 NN 검사가 돌아 orphan을 막는다.
18. **오버레이가 표를 덮는 z-index 구조 — 「첫 클릭이 씹힌다」의 유력 원인** ★ (2026-08-28 진단, 실측)

    | 오버레이 | z-index | 열리는 방식 |
    |---|---|---|
    | **GNB 드롭다운** | **949 / 950** | **hover로 열림**(진입 지연 120ms·이탈 지연 250ms) — #229·#231 |
    | **ColMenu 헤더 필터 메뉴** | **9998 / 9999** | 헤더 필터 아이콘 클릭 (`position: fixed`) |
    | **ColumnSelector**(컬럼 선택) | **9998 / 9999** | 버튼 클릭 |
    | **전역검색(GnbSearch)** | **9999** | 검색창 포커스·Ctrl+K |

    - **열린 상태에서 그 아래 표 영역을 클릭하면 오버레이가 클릭을 받는다.** 표는 클릭을 받지 못하고, **두 번째 클릭은 오버레이가 닫힌 뒤라 정상 동작** → 「첫 번째만 안 됨」.
    - **표가 아래로 밀린 화면일수록 덮이는 면적이 크다** — 약품목록·비보험·아카이브는 ATC 도넛 3종 + 필터 칩 4줄이 위에 얹혀 가장 취약.
    - ★ **바깥클릭 리스너가 클릭을 삼키는 것이 아니다.** 전수 확인 결과 바깥클릭 리스너는 **전부 「닫기만」 하는 비소비형**(`preventDefault`·`stopPropagation` 없음)이고, 실제 소비형은 훅A의 capture `click` 1건(모달 드래그 직후 1회 한정)뿐. **리스너를 뒤지지 말고 히트테스트(무엇이 그 자리를 덮고 있는가)를 볼 것.**
    - ※ GnbNav·날짜입력·DonutSetMenu·EmergencyDispense의 바깥클릭 리스너는 **열림 여부와 무관하게 항상 등록**되어 있으나(의존성 `[]`), 닫는 setState만 하므로 **무해**하다(이미 닫힘이면 React가 리렌더도 건너뜀).
19. **메모이제이션 전무 — 클릭 한 번이 전 트리 리렌더** (2026-08-28 진단, 실측) — `src/App.jsx` **5,182줄 전체에서 `useMemo` 0회·`useCallback` 0회**. 루트의 **컨텍스트 값이 매 렌더 새 객체**로 만들어져 **모든 소비자가 함께 리렌더**된다.
    - 약품 클릭은 **루트 상태**를 바꾸므로, 그 한 번이 ① 1,114건 필터+정렬 ② 도넛 3종 집계(1,114건 4회 순회) ③ 표 재조립 ④ **대형 편집 모달 신규 마운트**(수십 개 폼 필드 초기화)를 **모두 동기로** 유발한다. 이를 줄여 주는 장치가 **하나도 없다.**
    - 화면에 **누름 피드백·로딩 표시가 없어**, 지연이 한 프레임을 넘기면 사용자는 **미클릭으로 판단**한다([[함정 #18]]과 함께 §2 [판단 대기] 참조).
    - ※ **성능 최적화는 별건** — 착수 시 **회귀 범위가 크다**(컨텍스트 값 안정화는 전 화면 영향). 원인이 확정되기 전에 손대지 말 것.
20. **표 행 `key`가 인덱스 — 순서가 바뀌면 다른 약품이 열릴 위험** (2026-08-28 진단, 실측) — 약품목록 `<tbody>`의 행 key가 **페이지 내 인덱스**(`drug_code`가 아님). 정렬·필터·realtime 재조회로 **행 순서가 바뀌면 React가 같은 DOM 노드를 다른 약품에 재사용**하므로, 클릭 핸들러가 **의도와 다른 약품을 가리킬 수** 있다.
    - **현재 관측된 사고는 없으나 잠재 결함**이다. 행 수가 줄면 그 행이 제거되어 **클릭이 아예 유실**될 수도 있다(§2 [판단 대기] 3순위).
    - 수정은 key를 `drug_code`로 바꾸는 한 줄이지만, **다른 표에도 같은 패턴이 있어** 일괄 점검이 필요하다(별건).
21. **약품 검색 경로가 3갈래로 갈려 있다 — 「어느 화면에서 검색이 안 된다」는 버그가 아닐 수 있다** ★ (2026-08-28)

    | # | 경로 | 적용 화면 | 필드 |
    |---|---|---|---|
    | **①** | **`matchesDrugSearch`** (`src/lib/drugFilter.js`) | **약품목록 · 비보험 · 아카이브**(같은 `DrugList`) | **5개** — `drug_name`·`drug_code`·`ingredient_kr`·`manufacturer`·**`ingredient_en`**(PR #260) |
    | **②** | **`GLOBAL_SEARCH_FIELDS`** (같은 파일의 **별도 상수**) | **전역검색(Ctrl+K)** 전용 | 5개 — ①과 **목록이 다르게 정의돼 있었음**. ※ `ingredient_en`은 **여기엔 처음부터 있었다** |
    | **③** | **인라인 복제** | **향정마약 · 재고현황 · ATC** | 각 화면에 검색 로직이 복제돼 있어 **①·②의 변경이 자동 적용되지 않는다** |

    - ★ **「어느 화면에서 검색이 안 된다」는 문의는 버그가 아니라 경로별 범위 차이일 수 있다. 먼저 어느 경로인지 확인할 것.**
    - ※ **2026-08-28 「영문 성분명 검색 불가」가 정확히 이 사례** — ①에만 `ingredient_en`이 없었고 ②에는 있었다(§1 완료 이력 참조).

    **검색 필드 현황 (2026-08-28)**

    | 구분 | 필드 | 비고 |
    |---|---|---|
    | **적용 5개** | `drug_name` · `drug_code` · `ingredient_kr` · `manufacturer` · `ingredient_en` | 경로 ① 기준 |
    | **대기 6개** | `insurance_code` · `standard_code` · `atc_l2` · `atc_l3` · `specification` · `storage_location` | → **「첫 클릭 무반응」 계측 후 일괄**(성능 **+175%** 예상) |
    | **제외 확정** | **`atc_code`** — 「c」 1글자에 **316건**, 오탐 기여 최대 · **`atc_l1`** — 고유값 **15개**, 하나 걸리면 수백 건 · **`additive`·`memo`·`efficacy` 등 자유텍스트** — 오탐·비용 | 사용자 승인 |

    - ※ **최소 글자 수 제한 없음**(사용자 확정). **「c」 검색 시 약 353건**(기본 필터 「사용」 기준)은 **정상 동작**이며, 부분일치 검색의 성질이다(`drug_code`가 이미 같은 상태였다).
    - **부수 관측** — ⚠️ **`efficacy_class` 컬럼이 `drugs` 테이블에 존재하지 않는다.** 그런데 `App.jsx`에는 **10곳**(등록/수정 폼 상태·공공데이터 `divNm` 매핑·저장 payload)에 등장한다 → **저장돼도 컬럼이 없어 반영되지 않을 가능성**이 있다. **별도 확인 필요**(이번 범위 밖).
    - **값 0건 컬럼 7개**(검색·활용 검토 불필요): `indication` · `side_effects` · `dosage_form` · `standard` · `edi_code` · `dosage` · `notes`.
22. **저장 폴백이 저장 실패를 숨긴다 — 「미존재 컬럼 자동 제거」(0051 계열)** ★★ (2026-08-28)

    PostgREST가 「column ... does not exist」(PGRST204)를 반환하면, 저장 코드가 **오류 메시지에서 컬럼명을 뽑아 payload에서 그 키를 제거하고 재시도**한다(최대 3회). 재시도가 성공하므로 **저장은 「성공」으로 끝나고 사용자에게는 완료로 표시**된다. **흔적은 `console.warn` 한 줄뿐**이다.

    - **증상 패턴**: 화면에 **입력칸이 있고 값도 채워지는데**, 저장 후 재진입하면 **빈칸**. **오류도 안 뜨고**, 표에 그 컬럼이 없으면 **빈 값이 드러나지도 않는다.**
    - ★ **같은 유형이 세 번 발생했다**(필드 기준 3건 · PR 기준 2건):

      | # | 필드 | PR | 원인 유형 |
      |---|---|---|---|
      | 1 | `additive` | **#243** | 화면엔 입력칸이 있는데 **`ud` payload에 없어** 저장 안 됨 |
      | 2 | `compound_type` | **#243** | 동일 |
      | 3 | **`efficacy_class`** | **#262**(0081) | ★ **payload엔 있는데 DB 컬럼이 없었음** — 코드 10곳 완비, 컬럼만 부재 → **폴백이 은폐** |

    - ★ **「저장했는데 값이 사라진다」는 문의가 오면 이 순서로 볼 것**:
      1. **브라우저 콘솔**에서 **「미존재 컬럼 자동 제거」 경고** 확인 — 있으면 컬럼 부재 확정.
      2. 그 필드가 **DB 컬럼으로 실재**하는지 확인(`information_schema.columns`).
      3. **저장 payload에 포함**되는지 확인(INSERT `row` · UPDATE `ud`).
    - ※ **폴백 자체는 유지한다** — 일시적 스키마 불일치(마이그레이션 전후 등)에서 저장이 **통째로 실패하는 것을 막는 안전장치**다. 다만 **조용해서 결함을 숨긴다는 점을 알고 쓸 것.**
23. **마이그레이션 dryrun은 `authenticated` 롤로 할 것** ★ (2026-08-28, 0081 실경험) — **postgres 롤로 INSERT dryrun을 돌리면 `trg_set_tenant_id`가 발동하지 않아** `tenant_id` **NOT NULL(0059)** 에 걸려, **검증하려던 컬럼과 무관한 실패**가 난다(0081 첫 dryrun에서 실제로 걸림).
    - **해법**: `set local role authenticated` + `select set_config('request.jwt.claim.sub', <tenant_members.user_id>, true)` 를 설정한 뒤 INSERT할 것. **0080·0081 dryrun이 이 방식**이다.
    - ※ UPDATE·DDL 검증은 postgres 롤로도 무방하다. **문제가 되는 것은 tenant 트리거를 타야 하는 INSERT 경로**뿐.

---

## 6. 작업 원칙 · 프롬프트 규칙 · 표준 표 사양

### 작업 원칙 (표준 운영 규칙)
- **월마감·runClose는 사용자가 직접 실행** — 자동 실행 금지. monthly_snapshots·monthly_report_totals 생성·수정 금지(읽기만).
- **main 직접 커밋 금지** — feat 브랜치 → Deploy Preview → 정지. 병합은 사용자 승인 후.
- **마이그레이션은 dryrun(트랜잭션 내 검증→ROLLBACK) → 보고 → 승인 → apply**.
- **금액 산식·모집단·건수 소스 변경 금지**(표시 소스 정렬만). 기존 데이터 소급 정정은 별도 승인.
- 브랜드 4색: `#804A87`(보라)·`#019748`(녹색)·`#BFA6D9`(라벤더)·`#2E4A62`(네이비). 신규 색 금지. 폰트 Pretendard 유지.
- 데이터 변경 전 백업 CSV(Windows 경로, `.gitignore` 커버 `*_backup_*.csv`). 복원용 CSV 커밋 금지.
- 보안: `.env` 미추적(.env.example만), 프론트는 `VITE_SUPABASE_ANON_KEY`(공개 anon키)만. service_role 하드코딩 0.

### 배포 검증
- 해시 단독 판정 금지 → **로컬 빌드 해시 + 코드 마커 대조**. 마커 예: "실사 조정"·"재고 재평가"·"품목 · 중지 포함"·"관리 품목수 (사용·사용예정)"·"bulk_stock_adjust"·MAIN_ITEM_INGR 0건.

### 인쇄 영향 변경 시 육안 확인 (협업 프로토콜)
- Claude Code는 헤드리스라 **인쇄 미리보기를 렌더할 수 없다**. 아래 변경 시 프롬프트 [검증]에 「인쇄 3곳 육안 확인」을 넣고 **사용자 확인 요청 항목**으로 보고받는다.
  - **대상 변경**: 화면 제목·여백·컨테이너 패딩 / `@media print` 규칙 / 표 구조·컬럼·행 높이 / `#root` 폭·정렬
  - **확인 3곳**: ATC(#atc) · 보고서(#report) · 비상조제(#emergency)
  - **확인 항목**: 페이지 수 · 빈 페이지 · 잘림
- ※ 인쇄 화면이 3곳뿐이라 자동화(Playwright)보다 체크리스트가 저렴. 화면이 늘거나 회귀가 잦아지면 그때 자동화 검토.
- **[종결] 2026-08-24 인쇄 3곳 육안 확인 완료 — 회귀 0건** (ATC 2용지·헤더 매 페이지 반복·행 잘림 없음 / 비상조제 A4당 3×4=12매·2쪽·빈 페이지 없음 / 보고서 중간 여백 없음·정상).

### 모달 공통 사양 편차 (별건 후보 — 「모달 트랙」)
약 **12개 모달**에 **5축 편차**(화면 헤더 통일 PR #238과 같은 유형). 착수는 레이아웃 트랙 이후 권장.
- **제목**: fontSize 14~17 · weight 700/800 · 색 3종(t.text·t.accent·#fff) 혼재
- **닫기 ×**: 있음/없음/위치 제각각
- **하단 버튼**: 배치·개수·색 상이
- **오버레이**: rgba(0,0,0, 0.4/0.45/0.5) + transparent, 클릭 닫힘 일부 미지원
- **Esc**: 극소수만 지원(예: Drug360Modal)
- ※ 드래그 3종 병존(§5-13)도 함께 일원화 후보 — 통합 시 12모달 회귀 검증 수반.

### 약플로 표준 표 사양
- 정렬 가능한 헤더, 월·필터·정렬 변경 시 1페이지 리셋. 금액 셀 우측정렬·tabular-nums. 음수 괄호 표기(`_negWon`). 상태 배지(사용/중지/휴면).

---

## 7. 관련 브랜치/트랙
- 미배포 로컬: `feat/emergency-cutline-2mm`(재단선 2mm, 실물 대조 시에만).
- apply 대기: `chore/drop-junk-table-0058`(junk 테이블 DROP).

*(본 문서는 인계·기록용. 데이터·코드 변경 없음.)*