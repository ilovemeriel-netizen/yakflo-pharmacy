# 거래→재고 단일기록 일원화 설계 (0015) — 제안·미적용 (2026-06-23)

> 현재 transactions=0(거래 미개시)·0009 트리거 존재 → 일원화 적기. 본 문서는 **설계·영향범위 제안만**, 적용은 승인 후(Task3).
> 단일 정본 = drugs.current_qty. 모든 재고 이동은 transactions INSERT → 0009 트리거가 drugs+inventory 동기 갱신.

## 1) 비-트리거 재고변경 경로 전수 (src/App.jsx)
| 위치 | 동작 | 트리거 외 직접변경 | 이중기록 | 무음절삭 | 미러 desync |
|---|---|---|---|---|---|
| **1518** submit(단건) | tx insert 직후 `drugs.current_qty = client newQty` | ✔ | ✔(트리거와 동시) | — | drugs만 set, inventory는 트리거 |
| **1517** submit 출고 | `newQty = Math.max(0, old−q)` | — | — | **✔** 음수를 0으로 무음절삭 | — |
| **1561** bulkSubmit | 루프마다 동상(절삭+직접update) | ✔ | ✔ | ✔(출고) | drugs만 set |
| **1526** delTx | tx delete 후 `drugs.current_qty` 수동 복원 | ✔ | — (0009는 AFTER INSERT만) | — | inventory 미복원→desync |
| **557** AdjustModal(실사) | `drugs.current_qty=절대값`, inventory·tx 미변경 | ✔ | — | — | **✔** inventory desync |
| **373/378** EditModal(편집) | update payload에 `current_qty` 포함(절대 덮어쓰기) | ✔ | — | — | **✔** inventory desync |
| 1185/1237/1267 신규등록 | insert에 current_qty(개시재고) | (개시값·허용) | — | — | inventory 행 트리거가 별도 생성 |
| 792/799/860, v2 api.js:43 | expiry/note/status 등 비재고 필드 | — | — | — | — |

- **기준선(실측)**: drugs↔inventory 불일치 **42건 = 전부 0013 소수 보정분**(drugs 정수 287 vs inventory 286.5). transactions=0이라 이중기록 누적은 아직 0(깨끗한 일원화 가능).

## 2) 일원화 설계 (제안)
1. **삽입 경로(1518·1561)**: 클라이언트 `drugs.update({current_qty})` **제거** → 0009 트리거를 단일 기록자로. insert 성공 후 `onReload`로 트리거 결과 재조회.
2. **음수(1517·1561)**: `Math.max(0,…)` 절삭 **제거**, 실제 수량 전달 → 트리거의 `재고 부족` 예외(RAISE)로 **명시 차단**, 사용자에 오류 노출.
3. **삭제/복원(1526)**: 0015에 **AFTER DELETE 트리거** 추가(역델타로 drugs+inventory 복원) → 수동 복원 제거. (대안: append-only 역거래. 현 delTx UX 유지 위해 AFTER DELETE 권장.)
4. **실사 조정(557 AdjustModal)**: 직접 update 대신 **`transactions(type='조정', quantity=목표−현재)` INSERT** → 0009가 '조정' 처리(부호있는 차이 적용)로 drugs+inventory 동기. 사유는 tx.note/reason에 기록.
5. **편집(373 EditModal)**: update payload에서 **`current_qty` 제거**(재고는 편집으로 변경 불가, 거래/조정으로만).
6. **미러 정합(중요)**: `drugs.current_qty` integer→**numeric** 전환(0013로 inventory는 이미 numeric). 트리거의 `cur integer`→`cur numeric`. 이로써 소수 이동·기말이 양 테이블 동기 보존. (기준선 42건 불일치도 numeric 전환+동기화로 해소 가능)

## 3) 0015 마이그레이션 제안 SQL (미적용 — 승인 후 본 적용)
```sql
-- (a) apply_tx_to_inventory(): cur/delta numeric, '조정' 타입, 음수차단 유지
create or replace function public.apply_tx_to_inventory()
returns trigger language plpgsql security definer set search_path = public as $$
declare delta numeric; cur numeric;
begin
  delta := case new.type when '입고' then new.quantity when '출고' then -new.quantity
    when '폐기' then -new.quantity when '반품' then -new.quantity
    when '조정' then new.quantity   -- 조정: quantity=목표−현재(부호있는 차이)
    else 0 end;
  if delta < 0 then
    select current_qty into cur from public.inventory_stock where drug_code=new.drug_code and tenant_id=new.tenant_id;
    if coalesce(cur,0)+delta < 0 then
      raise exception '재고 부족: % (현재고 %, 차감요청 %)', new.drug_code, coalesce(cur,0), -delta using errcode='check_violation';
    end if;
  end if;
  if delta <> 0 then
    update public.inventory_stock set current_qty=coalesce(current_qty,0)+delta, updated_at=now()
      where drug_code=new.drug_code and tenant_id=new.tenant_id;
    if not found then insert into public.inventory_stock(drug_code,current_qty,tenant_id,drug_name)
      values(new.drug_code, greatest(delta,0), new.tenant_id,(select drug_name from public.drugs where drug_code=new.drug_code and tenant_id=new.tenant_id limit 1)); end if;
    update public.drugs set current_qty=coalesce(current_qty,0)+delta where drug_code=new.drug_code and tenant_id=new.tenant_id;
  end if;
  return new;
end $$;

-- (b) AFTER DELETE 역보정
create or replace function public.revert_tx_from_inventory()
returns trigger language plpgsql security definer set search_path = public as $$
declare delta numeric;
begin
  delta := case old.type when '입고' then -old.quantity when '출고' then old.quantity
    when '폐기' then old.quantity when '반품' then old.quantity
    when '조정' then -old.quantity else 0 end;
  if delta <> 0 then
    update public.inventory_stock set current_qty=coalesce(current_qty,0)+delta, updated_at=now() where drug_code=old.drug_code and tenant_id=old.tenant_id;
    update public.drugs set current_qty=coalesce(current_qty,0)+delta where drug_code=old.drug_code and tenant_id=old.tenant_id;
  end if;
  return old;
end $$;
drop trigger if exists trg_revert_tx_from_inventory on public.transactions;
create trigger trg_revert_tx_from_inventory after delete on public.transactions for each row execute function public.revert_tx_from_inventory();

-- (c) drugs.current_qty numeric (미러 정합·소수 보존)
alter table public.drugs alter column current_qty type numeric;
```
**롤백(역방향)**: 0009 원본 함수로 `create or replace` 복원 · `drop trigger trg_revert_tx_from_inventory` · `drop function revert_tx_from_inventory` · `alter table drugs alter column current_qty type integer using round(current_qty)::int`(⚠소수절삭).

## 4) 영향범위·검증 계획
- **수정 파일**: `src/App.jsx`(submit·bulkSubmit·delTx·AdjustModal·EditModal) — 가역(되돌림 커밋). v2(api.js insertTransaction)는 **이미 트리거 경유 → 무수정**. 대시보드/RPC는 drugs 조회 → 무영향.
- **회귀 0 기준**: lint 103e/8w, build. 레거시 입출고·실사·편집 동작은 결과 동일(트리거가 동일 델타 적용)하되 단일경로화.
- **사전 검증**: 적용 후 테스트 거래(입고/출고/조정/삭제) 1건씩 → drugs·inventory 동기·음수차단 RAISE 실증 → **테스트 거래 롤백/정리**(transactions 0 복귀).
- **위험**: drugs.current_qty numeric 전환 시 UI 정수 표기 확인(대시보드는 toLocaleString — 소수 표기 정책 필요). 거래 개시 전이라 데이터 영향 최소.

## 5) Q2·Q3·Q4 제안 (적용 보류)
### Q2 — safety 산출식(×0.5) 적정성 + 구분별 계수
- 구분별 적재 분포(현 ×0.5): 경구 278종 avg 161(max 7070) · 주사 39 avg 14 · 외용 46 avg 13 · 수액 10 avg 54 · 영양 11 avg 12.
- 관찰: 회전 빠른 경구는 ×0.5로 충분(고용량). **주사·응급·필수는 저빈도라 ×0.5면 과소**(avg 14) → 결품 위험.
- 제안(계수 차등, 미적용): 경구 ×0.5 / **주사·수액 ×0.75~1.0 + 최소 floor(예: 5)** / 외용 ×0.5. 표본 15종 실값을 약사 발주감각과 대조 후 확정.

### Q3 — 미적재 131종 분류(수기 기준 후보)
- 분포: 경구 75(고가≥1000원 9·**마약 4**) · 주사 25 · 외용 23 · 수액 6 · 영양 2.
- **우선 수기 대상(고가·필수)**: YZTNA560(영양 70,000원) · SEPL20(경구 23,906·재고1508) · SSYNYRXN8(13,877) · ETRST50(8,922) · CRTMP125(5,248) · ARGOTIN(5,000) … 및 **마약 4종**(규제·필수). → 이들 명단을 약무팀에 전달해 수기 safety 입력 권장. 나머지는 구분 기본값 또는 fallback 유지.

### Q4 — SACFN −305 알림 예외
- 음수재고가 부족 알림 deficit를 왜곡(safety 107−(−305)=412로 과대). 실사 확정일 미정.
- 제안(미적용): app_low_stock에 **`current_qty>=0` 조건 추가 또는 '실사대기' 플래그**로 SACFN을 알림에서 한시 예외. 실사 보정 시 예외 해제. (별도 RPC 0016 또는 파라미터화)

## 게이트(본 턴)
- **쓰기 0**(설계·제안만, 테스트 거래 없음) → 운영 무변경 · 회귀 0 · build 무영향 · 비밀 0. 적용은 승인 후 Task3.