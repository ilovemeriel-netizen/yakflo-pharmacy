# 재고·상태 정합 적용 결과 (2026-06-23, owner 로그인 성공 후)

> RLS owner 세션으로 적용. 가역(롤백 SQL 포함). 단일 정본 drugs 기준.

## status '??' 보정
- DWASPI100 `status='??'(hex 3f3f)` → `'사용'`. BEGIN/ROLLBACK 드라이런 검증 후 RLS 적용.
- 결과: 비정규 status 0건, 사용 517→518, 총 1109(KPI 분모 정합).
- 롤백: `update drugs set status='??' where tenant_id='<cnc>' and drug_code='DWASPI100';`

## safety_stock 적재 (하이브리드 자동)
- 기준: 최근 3개월(2026-03~05) 평균 사용량 × 0.5(반월)=safety, ×2=max. `scripts/compute_safety_stock.mjs --commit`.
- 적재: 사용 518종 중 **387종** safety/max 적재(가산적·미설정 행만).
- 제외: **131종**(사용량 0·산출 불가, 주로 수액·주사제) → 적재 제외(상수 fallback 10 유지). 향후 수기 또는 사용 누적 후 재산출.
- 검증(app_low_stock): 적재 약품은 실 safety로 동작(예: SACFN safety_stock=107, fallback 10 아님). 미적재 131종만 fallback 10.
- 롤백: `update drugs set safety_stock=0, max_stock=0 where tenant_id='<cnc>' and drug_code in (387종 — compute_safety_stock.mjs가 명단 출력);`

## 보류 유지
- **SACFN current_qty=−305**: 원본 일치(시드오류 아님) → 약무팀 실사값 수령 시 가역 보정. 현재 보류.
- **0009 트리거 일원화(0015)**: 다음 트랙(거래 개시 전). 제안만, 미적용.