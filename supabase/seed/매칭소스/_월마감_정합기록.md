# 월마감 2026-01~05 NOCODE 누락 정합 기록 (출처명 대조 + 정규화)

> 스크립트: `scripts/reconcile_monthly_nocode.mjs` (anon+RLS owner 세션, 미리보기→`--commit`)
> 원칙: 가산적·append-only(upsert만). 정규화·대조는 **비교 전용** — 원본 약품명/정본 값 변경 없음.
> 정책: **중복이 아닌 한 모든 기초데이터 등록**(누락 수용 안 함). 신규코드/등재 여부는 사용자 결정.

## 1) 매칭 단계 (우선순위)
① CSV drug_code → ② 약품명 정확일치 → ③ `norm1`(NFKC·공백·대소문자) → ④ `norm2`(기호 제거)
→ ⑤ `norm3`(**괄호 주석 제거**) → ⑥ `norm4`(**`/` 앞부분**). 다중 후보(모호)는 자동매칭 제외.
정규화는 비교 전용 — 원본/정본 무변경.

## 2) 회복 결과 (가산 upsert, 신규코드 0)
- **총 회복 +149행** (norm1/2 그대로 포함 후 norm3 +146 · norm4 +3). 전부 **기존 정본 코드**로 매칭(신규코드 없음).
- 핵심: 월마감명 뒤 변경/중단 주석 때문에 안 맞던 행을 괄호·`/` 제거로 회복.
  - 예: `디프렌캡슐20mg(로틴캡슐 변경예정)`→`디프렌캡슐20mg`(DPR20), `리큅정0.25mg(뉴큅정으로 변경)`→`리큅정0.25mg`(NOCODE-0021), `팔로델정/사용기한,사용량저조`→`팔로델정`(PLD2).
- 신규 (snap_month, drug_code) 키만 upsert. 재실행 시 회복 0행(멱등·가산 확인).

## 3) 완전성 표 (2026-06 = 1103 기준)
| 월 | CSV원천 | 적재(코드有) | 06대비격차 | 분해: 06신설 / 잔여(중지) / 구조적중복 |
|---|---|---|---|---|
| 2026-01 | 1070 | **1057** | 46 | 33 / 10 / 3 |
| 2026-02 | 1080 | **1071** | 32 | 23 / 9 / 0 |
| 2026-03 | 1087 | **1078** | 25 | 16 / 9 / 0 |
| 2026-04 | 1092 | **1083** | 20 | 11 / 9 / 0 |
| 2026-05 | 1103 | **1095** | 8 | 0 / 8 / 0 |
| 2026-06 | 1103 | 1103 | 0 | (yakflo_data 완전 시드, 무영향) |

`CSV원천 = 적재 + 잔여 + 구조적중복`(예: 01 → 1057+10+3=1070). **06신설** = 6월까지 추가된 약품(앞선 달엔 원래 없음).
**구조적중복** = 서로 다른 월마감명이 같은 기존 코드로 귀결돼 이미 적재됨(유니크 인덱스상 추가 불가 = "중복" 제외분).

## 4) 잔여 10종 (= 사용자 결정 필요)
잔여는 전부 거래·재고 0(중지). 정책상 **수용하지 않고 등록 대상**이나, 둘 다 결정이 필요해 보류 중:

### A. 모호 4종 — 정본에 동일/유사명 **복수 후보** (어느 코드인지 택1 필요)
| 월마감명 | 후보 코드 |
|---|---|
| 뮤테인캡슐200g(비엘아세틸캡슐 변경) | NOCODE-0043 / NOCODE-0044 (정본 동일명 2개) |
| 대한멸균생리식염수100ml/20개(병) | D0432(…100ml/20개) / NOCODE-0215(…(PP)) |
| 스카이셀플루4가PFS 0.5ml_(무료독감) | NOCODE-0256 / NOCODE-0257 (정본 동일명 2개) |
| 스카이셀플루4가PFS 0.5ml_(지자체) | NOCODE-0256 / NOCODE-0257 (정본 동일명 2개) |

### B. 정본 미존재 6종 — **신규 등재** 필요(새 코드 부여)
대화이자부프로펜400mg · 박씨그리프테트라주/독감 · 브로낙점안액 · 신풍로이솔주사액 · 에보프림 · 엘칸액

## 5) 가역성 (롤백)
회복분(149행)은 매칭된 (snap_year, snap_month, drug_code) 키 한정 delete로 제거 가능(append-only — delete-reload 미사용).
신규 등재분(승인 시) 적용 전 BEGIN/ROLLBACK 검증 + 롤백 SQL 동봉 예정.