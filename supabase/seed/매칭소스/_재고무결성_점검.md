# 재고 데이터 정합·무결성 점검 (2026-06-23)

> 조회: Management API 읽기(분석). 쓰기 보정은 **.owner-login.local 갱신 후 RLS 경유**로만 — 현재 갱신 미확인(LOGIN_FAIL)이라 **쓰기 미실행**.
> tenant=cnc(5e0aa267…). drugs 1109, inventory_stock current_qty numeric(0013).

## 1) 음수재고 전수·원인 분류 (Task1)
| drug_code | 약품명 | 상태 | drugs.current_qty | inventory.current_qty | monthly_06(원본) | 원인 |
|---|---|---|---|---|---|---|
| SACFN | 사푸틴정100mg | 사용 | **−305** | −305 | **−305** | **원본 자체 음수**(세 소스 일치) — 시드/매칭 오류 아님 |

- 음수 행: drugs 1건 · inventory 1건(동일 SACFN). 그 외 0.
- 원인 분류: **시드 오류 0 · 매칭 오류 0 · 원본 음수 1**. monthly 06(yakflo_data 유래)도 −305로 동일 → 적재는 원본을 정확히 반영. 원본(yakflo_data X열) 자체가 음수.

## 2) 보정 결과 (Task2)
- **가역 보정 대상 = 0건.** 유일 음수(SACFN)는 원본과 일치 → "오류 확인 행"이 아니므로 **자동 보정 안 함**(규칙 준수).
- **원인 불명/실사 필요 보류 명단**: `SACFN(−305)` — 현장 실사로 실재고 확정 후 거래(transactions) 또는 수기 보정 권장. 임의 수정 금지.
- ※ 쓰기 자체가 불필요했고, 설령 있었어도 .owner-login.local 갱신 전까지 RLS 쓰기는 미시작(사전조건).

## 3) safety_stock 산출 기준 제안 (Task3 — 제안만, 미적용)
현재 drugs·inventory_stock의 safety_stock 전부 0(미적재) → 대시보드 재고부족이 상수 fallback(10)에 의존.

| 방식 | 산출 | 장점 | 단점 |
|---|---|---|---|
| **A. 자동(권장 초기값)** | 최근 3개월(03~05) 평균 사용량 × 0.5(반월) = safety, ×2 = max | 데이터 기반·즉시·600종 일괄 | 신규/사용량 급변 약품 부정확 |
| **B. 수기** | 약사가 약품별 기준값 입력(설정 화면 연동) | 실무 정확 | 600+종 입력 부담 |
| **하이브리드(권장)** | A로 초기 일괄 → 핵심 약품만 B로 override | 균형 | — |

- 규모(실측): 사용 517종 중 **사용량>0 387종이 자동 safety 산출 대상**, 130종은 사용량 0(수기 필요).
- 샘플(평균사용/월 → safety,max): DPHALEYS5 14140 → 7070,28280 · Q-PAM100 10359 → 5180,20718 · MGM 3625 → 1813,7250 …
- 코드: `scripts/compute_safety_stock.mjs` 작성 완료(미리보기 기본). **실행 보류** — 기준 합의 + 자격증명 갱신 후 `--commit`. 가산적(미설정 행만)·롤백 명단 출력.

## 4) 재고 단일 정본 분석·통일 (Task4)
현 사용처:
| 위치 | 사용 컬럼 |
|---|---|
| 레거시 App.jsx(표시·거래) | `drugs.current_qty` (거래 시 line 1518 직접 update + 0009 트리거) |
| v2 InventoryPage / fetchInventoryList | `drugs.current_qty·safety_stock·max_stock` |
| /app 대시보드 RPC(app_low_stock·app_dashboard) | **`drugs`** (current_qty·safety_stock) |
| 유효임박 알림 | `drugs` |
| inventory_stock | 0009 트리거 미러 + safety/max/monthly_avg 보조 |

- **분석**: 모든 표시·집계 조회가 이미 `drugs.current_qty`를 정본으로 사용(대시보드 포함 — 재고부족도 drugs 기반). inventory_stock은 0009 트리거가 동기화하는 미러 + 일부 보조 컬럼.
- **통일안(제안)**: **drugs.current_qty를 공식 단일 정본**으로 명문화. inventory_stock.current_qty는 트리거 미러로 유지(또는 추후 제거 검토). safety_stock/max_stock도 정본=drugs로 통일(0014 RPC·v2 모두 drugs 사용 중이라 추가 변경 불필요).
- ⚠ 부채: 레거시 line 1518이 0009 트리거 원칙("trigger만 갱신")을 우회해 drugs.current_qty 직접 수정 → 거래 본격화 전 트리거 단일관문으로 일원화 권장(별도 작업).

## 5) KPI 상태분포 차이 규명 (Task5)
- 분포: 중지 584 · 사용 517 · 휴면 7 · **비정상 1** = 1109(총계 일치).
- 비정상 1행: **DWASPI100**(대웅바이오아스피린장용정100mg), `status` 값이 문자 그대로 **"??"**(hex `3f3f` = 물음표 2개), current_qty=1500.
- 원인: 원본 적재 시 상태 매핑 불가값이 `??`로 유입. 분모 차이(1108 vs 1109)의 정체.
- 제안(미적용): current_qty 1500·활성 정황상 `status='사용'`으로 보정 권장(단건). 쓰기는 자격증명 갱신 후 RLS 경유. KPI는 분모 명시(유효상태 1108 + 미분류 1)로 표기하거나 보정 후 정합.

## 게이트
- 쓰기 0(분석·제안만) → 운영 데이터 무변경. build 영향 없음(스키마·코드 변경 없음). lint 103e/8w 불변.
- 비밀 0(관리토큰 미노출). 신규 산출 스크립트는 `.mjs`(lint 비대상).